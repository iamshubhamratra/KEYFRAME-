---
version: alpha
name: Pipeline — Frame
description: >
  A working assembly line: product screenshots ride a conveyor past turning gears, a robotic arm and a stamping press, with throughput counters ticking. Industrial light grey and safety orange. For process, automation, CI/CD and anything built to scale.
unit: the frame — 1920×1080 primary; 9:16 supported
principle: the template's own grammar is sacred · only the words and pictures change
renderer: >
  Routes to the OMELETTE ADAPTER (server/src/services/omelette_adapter.js,
  pack.json "renderer": "omelette", "template": "Pipeline") — it renders the
  ORIGINAL Pipeline template rather than a re-implementation, driving it
  through the template's own frame-exact seek contract.
---

# Pipeline

Authored scene sequence, in order:

1. **Boot** — 4.5s
2. **Line** — 5.5s
3. **Inspect** — 5s
4. **Assemble** — 5s
5. **Throughput** — 4.5s
6. **Ship** — 5.5s

The adapter maps each storyboard beat onto this sequence, replacing copy and
media while preserving the template's shape. Durations come from the storyboard.

## Palette

Ground `#EAEDF2` · ink `#1E2230` · accent `#FF8A00`.

## Typography

**Caprasimo**, uppercase, weight 900 — the 2026-07-30 stomp/percussion wave.
