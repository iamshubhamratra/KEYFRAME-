---
name: vault-twelve
label: Vault Twelve
orientation: portrait
fontFamily: Chakra Petch
---

# Vault Twelve

Gunmetal (#141920) under a laser red (#ff3355), a bullion gold and a cold chrome. Chakra Petch sets technical headlines over IBM Plex Sans. The world is a vault door: a 132-radius dial in the lower right with twenty-four tick marks, every sixth one brighter, rotating back and forth as though someone is working the combination — it never completes, which is the joke. Its mechanics are a terminal writing code, a sweep ring, a keyboard cursor and dropping alerts. Best for security and fintech, infrastructure and access control, crypto custody and anything sold on being hard to get into.

## Palette

| role | hex |
|---|---|
| steel | `#141920` |
| laser | `#ff3355` |
| bullion | `#e8c35a` |
| chrome | `#c8d2dc` |
| ink | `#0a0d11` |

Ground `steel`, ink `ink`, paper `chrome`.
Brand accents take `laser`, `bullion`, `steel` in order; every other stop rotates onto the
brand's lead hue at its authored luminance.

## Type

Display **Chakra Petch** (sans-serif) · body **IBM Plex Sans**.
Headline tracking `0.04em`, leading `1.04`, average advance `0.52em`
(the fitter derives both the wrap target and the final size from that number).

## Motion

Camera set `zoomIn` · `pushR` · `spin` · `pushL` · `drop` · `zoomOut`, indexed `i * 7 + 2`.
Title entrance **machete**, item entrance **slam**.
World clock runs at `1.9x` film time.

## Beats

Six core beats — hook, statement, feature, montage, stats, cta.
This pack additionally owns:
- **Code** (`terminal`)
- **Ring** (`ring`)
- **Cursor** (`keys`)
- **Notify** (`drop`)

## World

The animated backdrop is the authored function, run verbatim per seek against an SVG-DOM
shim — the backdrop is not a reinterpretation of the original, it is the original.
