---
version: alpha
name: Fetch — Frame
description: >
  A playful sunlit park where a dog runs a ball back to its master, and product screenshots ride wooden signboards along the way. Warm cream, grass green, hot orange; motion is bouncy and character-led.
unit: the frame — 1920×1080 primary; 9:16 supported
principle: the template's own grammar is sacred · only the words and pictures change
renderer: >
  Routes to the OMELETTE ADAPTER (server/src/services/omelette_adapter.js,
  pack.json "renderer": "omelette", "template": "Fetch") — it renders the
  ORIGINAL Fetch template rather than a re-implementation, driving it
  through the template's own frame-exact seek contract.
---

# Fetch

Authored scene sequence, in order:

1. **Title** — 5s
2. **Run** — 6.5s
3. **Fetch** — 4.5s
4. **Feature** — 5.5s
5. **Stats** — 4.5s
6. **CTA** — 5.5s

The adapter maps each storyboard beat onto this sequence, replacing copy and
media while preserving the template's shape. Durations come from the storyboard.

## Palette

Ground `#FFFDF6` · ink `#3A352C` · accent `#F2683C`.

## Typography

**Orbitron**, uppercase, weight 900 — the 2026-07-30 stomp/percussion wave.
