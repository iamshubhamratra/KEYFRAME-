---
version: alpha
name: Hacker — Frame
description: >
  Matrix rain behind self-typing code, glitching titles and terminal windows under scanlines. Phosphor green on near-black with cyan and amber alarms. For security, infrastructure, developer tools and anything that should feel like the machine talking.
unit: the frame — 1920×1080 primary; 9:16 supported
principle: the template's own grammar is sacred · only the words and pictures change
renderer: >
  Routes to the OMELETTE ADAPTER (server/src/services/omelette_adapter.js,
  pack.json "renderer": "omelette", "template": "Hacker") — it renders the
  ORIGINAL Hacker template rather than a re-implementation, driving it
  through the template's own frame-exact seek contract.
---

# Hacker

Authored scene sequence, in order:

1. **Boot** — 4.5s
2. **Access** — 4.5s
3. **Compile** — 5.5s
4. **Deploy** — 5s
5. **Metrics** — 4.5s
6. **Run** — 5.5s

The adapter maps each storyboard beat onto this sequence, replacing copy and
media while preserving the template's shape. Durations come from the storyboard.

## Palette

Ground `#05080B` · ink `#D7F5E1` · accent `#37FF7A`.

## Typography

**Syne**, uppercase, weight 900 — the 2026-07-30 stomp/percussion wave.
