// BLITZ HOUR — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-4.js, template "BlitzHour").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display DM Serif Display / body DM Sans. Camera set ["zoomIn","pushR","spin","pushL","drop","pushU"] at stride 5/3.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "blitz-hour",
  label: "Blitz Hour",

  // ---- type ----
  display: "DM Serif Display", displayFallback: "Georgia, serif",
  body: "DM Sans", bodyFallback: "system-ui, sans-serif",
  em: 0.5831, bodyEm: 0.6179,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f0e9da", ink: "#1c1813", accent: t.accent || "#b3452e", accent2: t.accent2 || "#4d6b8c", sageT: "#e6eae4", terraT: "#f6e2d4" }),
  accents: ["accent","accent2"],
  accentOpts: ["#b3452e","#2e5fb3","#7a5c9e"],
  accent2Opts: ["#4d6b8c","#6b8c4d","#8c6b4d"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#14120f",

  // ---- camera + motion ----
  cams: ["zoomIn","pushR","spin","pushL","drop","pushU"],
  camStride: 5, camOff: 3,
  ambient: 1.8,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["BLITZ HOUR · A CHESS FILM","FIVE MINUTES EACH","THE CLOCK IS A PLAYER","TOUCH MOVE, NO ARGUMENTS","TUESDAY, EVERY WEEK"],
  foots: ["played at five minutes each, no increment","the flag fell in a winning position","notation abandoned around move twenty","biscuits consumed: many","the analysis lasted longer than the game"],
  sides: ["OPEN · PRESS · BLUNDER · SHAKE","FIVE MINUTES, NO INCREMENT","THE CLOCK NEVER BLUNDERS"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Blink"]],

  strings: {
    brandName: "Blitz Hour",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      (function () {
        const sq = 92, cells = [];
        for (let r = 0; r < 5; r++) for (let c = 0; c < 5; c++) if ((r + c) % 2 === 0) cells.push(R("rect", { key: r + "-" + c, x: u.W - 500 + c * sq, y: u.H - 500 + r * sq, width: sq, height: sq, fill: rgba(th.accent2, 0.3) }));
        return R("g", null, cells, R("rect", { x: u.W - 512, y: u.H - 512, width: sq * 5 + 24, height: sq * 5 + 24, fill: "none", stroke: rgba(th.ink, u.dark ? 0.4 : 0.45), strokeWidth: 7 }));
      })(),
      (function () {
        const hop = (t * 0.5) % 1;
        const moves = [[0, 0], [1, 2], [3, 3], [2, 1], [0, 0]];
        const mi = Math.min(3, Math.floor(hop * 4)), mf = hop * 4 - mi;
        const e = u.ease.inOut(u.clamp01(mf));
        const x = u.W - 500 + (moves[mi][0] + (moves[mi + 1][0] - moves[mi][0]) * e) * 92 + 46;
        const y = u.H - 500 + (moves[mi][1] + (moves[mi + 1][1] - moves[mi][1]) * e) * 92 + 46 - Math.sin(e * Math.PI) * 70;
        return R("path", { d: "M -20 28 h40 M -16 22 h32 l-5 -20 9 -13 -11 -20 -18 9 5 15 -9 11 z", fill: th.accent, stroke: rgba(th.ink, 0.8), strokeWidth: 3, strokeLinejoin: "round", transform: "translate(" + x + "," + y + ")" });
      })(),
      (function () {
        const flag = Math.sin(t * 0.8) > 0.5;
        return R("g", { transform: "translate(220,300)" },
          R("rect", { x: -180, y: -80, width: 360, height: 170, rx: 16, fill: rgba(th.ink, u.dark ? 0.5 : 0.12), stroke: rgba(th.ink, 0.4), strokeWidth: 5 }),
          [[-90, 0], [90, 1]].map(([ox, k]) => R("g", { key: k, transform: "translate(" + ox + ",10)" },
            R("circle", { r: 62, fill: rgba(th.paper, u.dark ? 0.15 : 0.85), stroke: rgba(th.ink, 0.5), strokeWidth: 4 }),
            R("line", { x1: 0, y1: 0, x2: Math.cos(t * (k ? 2.4 : 0.5) - 1.57) * 46, y2: Math.sin(t * (k ? 2.4 : 0.5) - 1.57) * 46, stroke: k ? th.accent : rgba(th.ink, 0.7), strokeWidth: 5, strokeLinecap: "round" }),
            k === 1 && flag ? R("rect", { x: -8, y: -74, width: 16, height: 16, fill: th.accent }) : null)));
      })(),
      Array.from({ length: 4 }).map((_, k) => R("text", { key: "p" + k, x: 130 + k * 70, y: u.H - 180, fontFamily: '"DM Serif Display", serif', fontSize: 54, fill: rgba(th.ink, u.dark ? 0.3 : 0.25 + (Math.floor(t * 0.8) % 4 === k ? 0.4 : 0)) }, ["\u265f", "\u265e", "\u265d", "\u265b"][k]))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
