// CANOPY — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-2.js, template "Canopy").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Vollkorn / body Alegreya Sans. Camera set ["zoomIn","pushD","hopU","pushL","zoomOut","pushR"] at stride 5/1.
// 40 scenes, 300.5s, the canonical spine unmodified.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "canopy",
  label: "Canopy",

  // ---- type ----
  display: "Vollkorn", displayFallback: "Georgia, serif",
  body: "Alegreya Sans", bodyFallback: "system-ui, sans-serif",
  em: 0.6229, bodyEm: 0.528,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#eef0e2", ink: "#16241c", accent: t.accent || "#d98c2c", accent2: t.accent2 || "#3f8c5f", sageT: "#e4ece2", terraT: "#f4e6cf" }),
  accents: ["accent","accent2"],
  accentOpts: ["#d98c2c","#c45f3c","#c4a02c"],
  accent2Opts: ["#3f8c5f","#2c7a7a","#6b8c3f"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#0f1a14",

  // ---- camera + motion ----
  cams: ["zoomIn","pushD","hopU","pushL","zoomOut","pushR"],
  camStride: 5, camOff: 1,
  ambient: 1.5,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["CANOPY · A RAINFOREST FILM","THIRTY METRES UP","NINETY PERCENT HUMIDITY","LOUDER THAN A CITY","WALK SLOWLY"],
  foots: ["recorded from a rope platform","everything here is competing for light","the ants run the whole operation","rain arrives at four, reliably","one hectare, four hundred species"],
  sides: ["LOOK UP · STEP SLOWLY","FOUR LAYERS OF FOREST","RAIN AT FOUR, DAILY"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [],

  strings: {
    brandName: "Canopy",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      Array.from({ length: 4 }).map((_, k) => R("polygon", { key: "r" + k, points: (200 + k * 420) + ",-20 " + (330 + k * 420) + ",-20 " + (120 + k * 420 + Math.sin(t * 0.2 + k) * 50) + "," + u.H + " " + (30 + k * 420 + Math.sin(t * 0.2 + k) * 50) + "," + u.H, fill: rgba("#ffeeb0", u.dark ? 0.04 : 0.1) })),
      [0, 1, 2, 3, 4, 5].map((k) => {
        const sway = Math.sin(t * 0.8 + k * 1.1) * 7;
        const x = (k % 2 ? 90 : u.W - 90), y = 120 + Math.floor(k / 2) * 210;
        return R("g", { key: "l" + k, transform: "translate(" + x + "," + y + ") rotate(" + (sway + (k % 2 ? 20 : -20)) + ")" },
          R("path", { d: "M 0 0 q 130 -70 250 -6 q -130 76 -250 6 z", fill: rgba(th.accent2, 0.5 - (k % 3) * 0.1), transform: k % 2 ? "" : "scale(-1,1)" }),
          R("path", { d: "M 0 0 q 130 -34 248 -3", fill: "none", stroke: rgba(th.ink, 0.25), strokeWidth: 4, transform: k % 2 ? "" : "scale(-1,1)" }));
      }),
      Array.from({ length: 8 }).map((_, k) => {
        const ph = (t * (0.3 + (k % 3) * 0.12) + k * 0.12) % 1;
        return R("ellipse", { key: "d" + k, cx: (k * 233 + 120) % u.W, cy: ph * (u.H + 60) - 30, rx: 4, ry: 12, fill: rgba(th.paper, 0.4 * (1 - ph * 0.5)) });
      }),
      (function () {
        const sw = Math.sin(t * 0.9) * 20;
        return R("g", { transform: "translate(" + (u.W - 320) + ",120) rotate(" + sw + ")" },
          R("path", { d: "M 0 0 q 20 200 -10 320", fill: "none", stroke: rgba(th.accent2, 0.7), strokeWidth: 12, strokeLinecap: "round" }),
          [0.35, 0.6, 0.85].map((f, j) => R("path", { key: j, d: "M " + (f * 14) + " " + (f * 320) + " q 60 -26 96 6", fill: "none", stroke: rgba(th.accent2, 0.6), strokeWidth: 9, strokeLinecap: "round" })));
      })(),
      R("path", { d: "M -20 " + (u.H - 100) + " q 300 -60 600 -10 q 340 46 700 -34 L " + (u.W + 20) + " " + (u.H + 20) + " L -20 " + (u.H + 20) + " Z", fill: rgba(th.ink, 0.3) })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
