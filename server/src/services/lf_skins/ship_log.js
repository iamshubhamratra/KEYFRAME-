// SHIP LOG — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-1.js, template "ShipLog").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Space Grotesk / body IBM Plex Sans. Camera set ["zoomIn","pushL","pushD","pushR","zoomOut","spin"] at stride 3/1.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "ship-log",
  label: "Ship Log",

  // ---- type ----
  display: "Space Grotesk", displayFallback: "system-ui, sans-serif",
  body: "IBM Plex Sans", bodyFallback: "system-ui, sans-serif",
  em: 0.6208, bodyEm: 0.6243,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#e6edf3", ink: "#0d1117", accent: t.accent || "#4ad2a0", accent2: t.accent2 || "#6a8cff", sageT: "#161d24", terraT: "#1a1620" }),
  accents: ["accent","accent2"],
  accentOpts: ["#4ad2a0","#f2b03c","#ff6b8a"],
  accent2Opts: ["#6a8cff","#b06aff","#4ac2d2"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#0b0f14",

  // ---- camera + motion ----
  cams: ["zoomIn","pushL","pushD","pushR","zoomOut","spin"],
  camStride: 3, camOff: 1,
  ambient: 1.6,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["SHIP LOG · BUILD FILM","MAIN IS GREEN","SMALL COMMITS, OFTEN","DEPLOY ON A FRIDAY. WE DARE YOU.","TESTS BEFORE VIBES"],
  foots: ["every number here came from ci","the graph is real, the bugs were too","rollback took eleven seconds","written between two deploys","yes, it was a missing semicolon"],
  sides: ["COMMIT · REVIEW · SHIP","MEAN TIME TO RECOVERY: LOW","NO MANUAL STEPS LEFT"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Blink"]],

  strings: {
    brandName: "Ship Log",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      Array.from({ length: 5 }).map((_, k) => R("line", { key: "g" + k, x1: 0, y1: 200 + k * 190, x2: u.W, y2: 200 + k * 190, stroke: rgba(th.paper, 0.05), strokeWidth: 2 })),
      (function () {
        const nodes = 9, drw = (t * 0.14) % 1.2;
        return R("g", { transform: "translate(" + (u.W - 620) + "," + (u.H - 420) + ")" },
          R("path", { d: "M 0 200 " + Array.from({ length: nodes }).map((_, k) => "L " + (k * 68 + 68) + " " + (200 - ((k * 37) % 130))).join(" "), fill: "none", stroke: rgba(th.accent, 0.6), strokeWidth: 5, strokeDasharray: 900, strokeDashoffset: 900 * (1 - Math.min(1, drw)) }),
          R("path", { d: "M 136 " + (200 - 37) + " q 80 -80 200 -30 q 90 40 190 -50", fill: "none", stroke: rgba(th.accent2, 0.5), strokeWidth: 4, strokeDasharray: "12 10" }),
          Array.from({ length: nodes }).map((_, k) => R("circle", { key: k, cx: k * 68 + 68, cy: 200 - ((k * 37) % 130), r: (k / nodes < drw) ? 11 : 5, fill: k % 3 === 0 ? th.accent2 : th.accent, opacity: (k / nodes < drw) ? 1 : 0.3 })));
      })(),
      (function () {
        const lines = ["$ git push origin main", "\u2192 running 214 tests \u2026", "\u2713 all green in 38s", "\u2192 deploying to prod", "\u2713 live"];
        const shown = Math.floor(((t * 0.5) % 1.4) * lines.length);
        return R("g", { transform: "translate(120," + (u.H - 500) + ")" },
          R("rect", { x: -30, y: -50, width: 660, height: 330, rx: 18, fill: rgba(th.paper, 0.05), stroke: rgba(th.paper, 0.14), strokeWidth: 3 }),
          [0, 1, 2].map((k) => R("circle", { key: k, cx: 6 + k * 30, cy: -22, r: 8, fill: rgba([th.accent, "#f2b03c", "#ff6b8a"][k], 0.8) })),
          lines.map((ln, k) => k <= shown ? R("text", { key: k, x: 6, y: 40 + k * 48, fontFamily: '"IBM Plex Sans", monospace', fontSize: 28, fill: rgba(k === shown ? th.accent : th.paper, k === shown ? 0.95 : 0.4) }, ln) : null),
          R("rect", { x: 6, y: 18 + shown * 48, width: 14, height: 30, fill: th.accent, opacity: Math.sin(t * 7) > 0 ? 0.9 : 0 }));
      })(),
      Array.from({ length: 4 }).map((_, k) => {
        const q = ((t * 0.4 + k * 0.25) % 1);
        return R("rect", { key: "p" + k, x: q * u.W - 40, y: 170, width: 40, height: 8, rx: 4, fill: rgba(k % 2 ? th.accent2 : th.accent, 0.7) });
      }),
      R("line", { x1: 0, y1: 174, x2: u.W, y2: 174, stroke: rgba(th.paper, 0.12), strokeWidth: 3 })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
