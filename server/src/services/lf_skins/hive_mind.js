// HIVE MIND — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-3.js, template "HiveMind").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Fredoka / body Quicksand. Camera set ["hopU","pushR","zoomIn","pushL","drop","zoomOut"] at stride 3/1.
// 41 scenes, 308s, 2 spine overrides.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "hive-mind",
  label: "Hive Mind",

  // ---- type ----
  display: "Fredoka", displayFallback: "Georgia, serif",
  body: "Quicksand", bodyFallback: "system-ui, sans-serif",
  em: 0.6204, bodyEm: 0.6165,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#fdf4dd", ink: "#33280f", accent: t.accent || "#e0a92c", accent2: t.accent2 || "#7a8c3f", sageT: "#eceedc", terraT: "#faead0" }),
  accents: ["accent","accent2"],
  accentOpts: ["#e0a92c","#c48a2c","#d9762c"],
  accent2Opts: ["#7a8c3f","#4d7a5f","#8c6b3f"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#2e2410",

  // ---- camera + motion ----
  cams: ["hopU","pushR","zoomIn","pushL","drop","zoomOut"],
  camStride: 3, camOff: 1,
  ambient: 1.7,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["HIVE MIND · A BEE FILM","FIFTY THOUSAND STAFF","SMOKER LIT AT NINE","THE QUEEN DECIDES","HONEY IS A BYPRODUCT"],
  foots: ["filmed with the veil on, mostly","stings this season: eleven, all deserved","one hive, three million flowers","the smoker went out twice","frames lifted slowly, always"],
  sides: ["OPEN · CHECK · CLOSE","NEVER RUSH A FRAME","FIFTY THOUSAND, ONE MIND"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 41 scenes.
  spineOverrides: [["insert",36,"Patience"],[2,"Blink"]],

  strings: {
    brandName: "Hive Mind",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      (function () {
        const hex = (cx, cy, r) => { let d = ""; for (let i = 0; i < 6; i++) { const a = Math.PI / 3 * i - Math.PI / 6; d += (i ? "L" : "M") + (cx + Math.cos(a) * r) + " " + (cy + Math.sin(a) * r) + " "; } return d + "Z"; };
        const cells = [];
        for (let row = 0; row < 4; row++) for (let col = 0; col < 3; col++) {
          const cx = u.W - 300 + col * 96, cy = 200 + row * 84 + (col % 2) * 42;
          const ph = ((t * 0.3 + row * 0.2 + col * 0.14) % 1.6);
          cells.push(R("path", { key: row + "-" + col, d: hex(cx, cy, 50), fill: ph < 0.8 ? rgba(th.accent, 0.5) : "none", stroke: rgba(th.accent, 0.6), strokeWidth: 5 }));
        }
        return R("g", null, cells);
      })(),
      [0, 1, 2].map((k) => {
        const a = t * (1.1 + k * 0.3) + k * 2.4;
        const bx = 300 + Math.sin(a) * 220, by = u.H - 420 + Math.sin(a * 2) * 120;
        return R("g", { key: "bee" + k, transform: "translate(" + bx + "," + by + ") rotate(" + Math.cos(a) * 24 + ")" },
          R("ellipse", { rx: 22, ry: 15, fill: th.accent, stroke: rgba(th.ink, 0.8), strokeWidth: 4 }),
          R("path", { d: "M -7 -13 v26 M 5 -14 v28", stroke: rgba(th.ink, 0.8), strokeWidth: 4 }),
          R("ellipse", { cx: -3, cy: -19, rx: 12, ry: 8, fill: rgba("#ffffff", 0.7) }));
      }),
      [0, 1, 2].map((k) => {
        const ph = (t * 0.2 + k * 0.33) % 1;
        return R("path", { key: "sm" + k, d: "M " + (160 + k * 60) + " " + (u.H - 200 - ph * 420) + " q 22 -40 0 -80 q -22 -40 0 -80", fill: "none", stroke: rgba(th.paper, 0.18 * (1 - ph)), strokeWidth: 14, strokeLinecap: "round" });
      }),
      R("rect", { x: 120, y: u.H - 220, width: 180, height: 150, rx: 12, fill: rgba(th.ink, 0.35) }),
      R("path", { d: "M -20 " + (u.H - 90) + " q 300 -50 620 -14 q 340 32 700 -30 L " + (u.W + 20) + " " + (u.H + 20) + " L -20 " + (u.H + 20) + " Z", fill: rgba(th.accent2, 0.22) })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
