// SPLIT TIMES — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-2.js, template "SplitTimes").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Big Shoulders Display / body Red Hat Text. Camera set ["pushL","pushR","zoomIn","hopU","pushU","spin"] at stride 7/2.
// 41 scenes, 308s, 2 spine overrides.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "split-times",
  label: "Split Times",

  // ---- type ----
  display: "Big Shoulders Display", displayFallback: "system-ui, sans-serif",
  body: "Red Hat Text", bodyFallback: "system-ui, sans-serif",
  em: 0.3803, bodyEm: 0.6486,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#eef3f6", ink: "#141a20", accent: t.accent || "#ff5a3c", accent2: t.accent2 || "#2ba3c4", sageT: "#e2eef0", terraT: "#f7e3dc" }),
  accents: ["accent","accent2"],
  accentOpts: ["#ff5a3c","#e8d84b","#ff3c78"],
  accent2Opts: ["#2ba3c4","#2c7a5f","#5a5ac4"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#101418",

  // ---- camera + motion ----
  cams: ["pushL","pushR","zoomIn","hopU","pushU","spin"],
  camStride: 7, camOff: 2,
  ambient: 2,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["SPLIT TIMES · A RUNNING FILM","EVERY KILOMETRE COUNTED","NEGATIVE SPLITS ONLY","THE WATCH DOES NOT LIE","TRACK TUESDAY"],
  foots: ["filmed at 4:52 per kilometre","the last lap was the fastest","shoes replaced at 700 km","nobody here is fast, everybody is faster","splits taken by hand, then by watch"],
  sides: ["WARM UP · WORK · COOL DOWN","EFFORT OVER PACE","SIX HUNDRED METRES TO GO"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 41 scenes.
  spineOverrides: [["insert",36,"Patience"],[2,"Rain"]],

  strings: {
    brandName: "Split Times",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("path", { d: "M -60 " + (u.H - 210) + " Q " + (u.W / 2) + " " + (u.H - 330) + " " + (u.W + 60) + " " + (u.H - 190), fill: "none", stroke: rgba(th.accent, u.dark ? 0.14 : 0.2), strokeWidth: 120 }),
      Array.from({ length: 8 }).map((_, k) => {
        const q = ((k / 8 + t * 0.07) % 1);
        return R("rect", { key: k, x: q * (u.W + 60) - 60, y: u.H - 236 - Math.sin(q * Math.PI) * 62, width: 54, height: 9, rx: 5, fill: rgba(th.paper, 0.6) });
      }),
      (function () {
        const bob = Math.abs(Math.sin(t * 6.5)) * 15, sw = Math.sin(t * 6.5);
        const rx = 260 + Math.sin(t * 0.4) * 40, ry = u.H - 380 - bob;
        return R("g", { transform: "translate(" + rx + "," + ry + ")" },
          R("circle", { cy: -66, r: 17, fill: th.paper }),
          R("path", { d: "M 0 -48 L -5 10 M 0 -34 l -30 " + (12 + sw * 14) + " M 0 -34 l 28 " + (12 - sw * 14) + " M -5 10 l -20 " + (30 + sw * 12) + " M -5 10 l 25 " + (30 - sw * 12), stroke: th.paper, strokeWidth: 10, strokeLinecap: "round", fill: "none" }),
          R("rect", { x: -18, y: -50, width: 36, height: 24, rx: 9, fill: th.accent }));
      })(),
      R("g", { transform: "translate(" + (u.W - 210) + ",250)" },
        R("rect", { x: -108, y: -54, width: 216, height: 108, rx: 22, fill: rgba(th.ink, u.dark ? 0.5 : 0.14), stroke: rgba(th.paper, u.dark ? 0.3 : 0.2), strokeWidth: 4 }),
        R("text", { y: 16, textAnchor: "middle", fontFamily: '"Big Shoulders Display", sans-serif', fontSize: 62, fill: th.accent }, "4:" + String(48 + Math.floor(Math.abs(Math.sin(t * 0.5)) * 9)).padStart(2, "0"))),
      Array.from({ length: 4 }).map((_, k) => R("line", { key: "s" + k, x1: 90 + ((t * 300 + k * 200) % 620), y1: 300 + k * 60, x2: 200 + ((t * 300 + k * 200) % 620), y2: 300 + k * 60, stroke: rgba(th.accent2, 0.4 - k * 0.07), strokeWidth: 8 - k, strokeLinecap: "round" }))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
