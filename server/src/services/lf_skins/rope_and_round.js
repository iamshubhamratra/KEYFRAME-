// ROPE & ROUND — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-5.js, template "RopeRound").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Archivo Black / body Archivo. Camera set ["pushR","zoomIn","spin","pushL","hopU","drop"] at stride 7/2.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "rope-and-round",
  label: "Rope & Round",

  // ---- type ----
  display: "Archivo Black", displayFallback: "system-ui, sans-serif",
  body: "Archivo", bodyFallback: "system-ui, sans-serif",
  em: 0.7408, bodyEm: 0.6506,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f2f0ec", ink: "#17171a", accent: t.accent || "#e03a3a", accent2: t.accent2 || "#c9a52c", sageT: "#e6e8ea", terraT: "#f6e0e0" }),
  accents: ["accent","accent2"],
  accentOpts: ["#e03a3a","#3a6be0","#e07a2c"],
  accent2Opts: ["#c9a52c","#8c8c8c","#2ca57a"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#141416",

  // ---- camera + motion ----
  cams: ["pushR","zoomIn","spin","pushL","hopU","drop"],
  camStride: 7, camOff: 2,
  ambient: 2,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["ROPE & ROUND · A BOXING FILM","THREE MINUTES, ONE MINUTE","HANDS UP, CHIN DOWN","NOBODY SPARS ON WEEK ONE","THE BELL DECIDES"],
  foots: ["filmed across twelve rounds","wraps done properly, every session","the bag has been rehung four times","nobody here fights, most of them","the bell is louder than it needs to be"],
  sides: ["WRAP · SKIP · BAG · BREATHE","THREE ON, ONE OFF","HANDS UP, CHIN DOWN"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Rain"]],

  strings: {
    brandName: "Rope & Round",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      (function () {
        const sw = Math.sin(t * 1.8) * 22;
        return R("g", { transform: "translate(" + (u.W - 260) + ",120) rotate(" + sw + ")" },
          R("line", { x1: 0, y1: 0, x2: 0, y2: 150, stroke: rgba(th.paper, 0.4), strokeWidth: 6 }),
          R("rect", { x: -58, y: 150, width: 116, height: 330, rx: 54, fill: rgba(th.ink, 0.85) }),
          R("rect", { x: -58, y: 220, width: 116, height: 30, fill: rgba(th.accent, 0.85) }),
          R("rect", { x: -58, y: 380, width: 116, height: 30, fill: rgba(th.accent, 0.85) }));
      })(),
      (function () {
        const a = t * 7;
        const ry = Math.abs(Math.sin(a)) * 130;
        return R("g", { transform: "translate(320," + (u.H - 300) + ")" },
          R("path", { d: "M -90 0 Q 0 " + (160 - ry) + " 90 0", fill: "none", stroke: rgba(th.accent2, 0.8), strokeWidth: 6 }),
          R("path", { d: "M -90 0 Q 0 " + (-40 - ry * 0.5) + " 90 0", fill: "none", stroke: rgba(th.accent2, 0.35), strokeWidth: 5 }),
          R("circle", { cy: -170 + Math.abs(Math.sin(a)) * 12, r: 18, fill: rgba(th.paper, 0.7) }),
          R("rect", { x: -14, y: -152, width: 28, height: 120, rx: 12, fill: rgba(th.paper, 0.7) }));
      })(),
      [0, 1, 2].map((k) => R("line", { key: "rp" + k, x1: -20, y1: u.H - 380 + k * 80, x2: u.W + 20, y2: u.H - 380 + k * 80 + Math.sin(t * 1.2 + k) * 6, stroke: rgba(th.accent, 0.28), strokeWidth: 12 })),
      (function () {
        const ring = Math.sin(t * 0.6) > 0.9;
        return R("g", { transform: "translate(" + (u.W - 700) + ",230)" },
          R("path", { d: "M -46 20 a 46 46 0 0 1 92 0 z", fill: rgba(th.accent2, ring ? 0.95 : 0.5) }),
          R("rect", { x: -54, y: 20, width: 108, height: 12, rx: 6, fill: rgba(th.accent2, ring ? 0.95 : 0.5) }),
          R("circle", { cy: 40 + (ring ? 6 : 0), r: 10, fill: rgba(th.accent2, ring ? 0.95 : 0.5) }));
      })(),
      Array.from({ length: 4 }).map((_, k) => R("rect", { key: "gl" + k, x: 120 + k * 90, y: u.H - 170, width: 62, height: 46, rx: 20, fill: rgba(k % 2 ? th.accent : th.ink, 0.55) }))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
