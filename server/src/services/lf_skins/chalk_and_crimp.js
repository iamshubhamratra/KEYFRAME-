// CHALK & CRIMP — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-4.js, template "ChalkCrimp").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Bebas Neue / body Instrument Sans. Camera set ["pushU","zoomIn","pushL","hopU","pushR","drop"] at stride 7/1.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "chalk-and-crimp",
  label: "Chalk & Crimp",

  // ---- type ----
  display: "Bebas Neue", displayFallback: "system-ui, sans-serif",
  body: "Instrument Sans", bodyFallback: "system-ui, sans-serif",
  em: 0.3922, bodyEm: 0.6544,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f2f2ee", ink: "#1b1e22", accent: t.accent || "#ff8a3d", accent2: t.accent2 || "#3d7bff", sageT: "#e6ecec", terraT: "#f7e6d8" }),
  accents: ["accent","accent2"],
  accentOpts: ["#ff8a3d","#c3f53c","#ff5d8f"],
  accent2Opts: ["#3d7bff","#3dc4b8","#8a3dff"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#181a1e",

  // ---- camera + motion ----
  cams: ["pushU","zoomIn","pushL","hopU","pushR","drop"],
  camStride: 7, camOff: 1,
  ambient: 1.9,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["CHALK & CRIMP · A CLIMBING FILM","PROJECT: ATTEMPT 19","FEET FIRST, ALWAYS","SKIN IS THE LIMIT","RESET ON FRIDAYS"],
  foots: ["filmed from the mats, looking up","attempt nineteen, still not sent","the beta was wrong for four sessions","chalk everywhere, permanently","the crux is a foot move, not a pull"],
  sides: ["WARM · PROJECT · REST","FEET BEFORE FINGERS","ATTEMPT NINETEEN, PATIENTLY"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Rain"]],

  strings: {
    brandName: "Chalk & Crimp",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("path", { d: "M " + (u.W - 520) + " -20 L " + (u.W - 300) + " " + (u.H * 0.45) + " L " + (u.W - 440) + " " + u.H + " L " + (u.W + 20) + " " + u.H + " L " + (u.W + 20) + " -20 Z", fill: rgba(th.ink, u.dark ? 0.45 : 0.14) }),
      [[u.W - 420, 200, 0], [u.W - 340, 380, 1], [u.W - 430, 560, 2], [u.W - 300, 720, 3], [u.W - 400, 880, 4]].map(([x, y, i]) => {
        const ph = ((t * 0.35 + i * 0.18) % 1.6);
        const e = ph < 1 ? Math.min(1, ph * 3) : 1;
        const shapes = ["M -26 10 q 26 -34 52 0 z", "M -22 -14 h 44 v 28 h -44 z", "M 0 -24 l 24 40 h -48 z", "M -26 0 a 26 20 0 1 0 52 0 a 26 20 0 1 0 -52 0", "M -24 12 q 10 -40 48 -6 q -20 24 -48 6"];
        return R("path", { key: i, d: shapes[i], fill: [th.accent, th.accent2, th.paper, th.accent, th.accent2][i], transform: "translate(" + x + "," + y + ") scale(" + e + ") rotate(" + i * 24 + ")", opacity: 0.9 });
      }),
      (function () { const puff = (t * 0.5) % 1; return R("circle", { cx: u.W - 380, cy: 560, r: 10 + puff * 50, fill: rgba(th.paper, 0.22 * (1 - puff)) }); })(),
      R("path", { d: "M 200 -20 q " + (Math.sin(t * 1.1) * 50) + " 320 0 640", fill: "none", stroke: rgba(th.paper, u.dark ? 0.3 : 0.35), strokeWidth: 7, strokeLinecap: "round" }),
      R("circle", { cx: 200 + Math.sin(t * 1.1) * 12, cy: 620, r: 18, fill: "none", stroke: th.accent, strokeWidth: 7 }),
      Array.from({ length: 5 }).map((_, k) => R("rect", { key: "p" + k, x: 90 + k * 130, y: u.H - 150, width: 110, height: 40, rx: 10, fill: rgba(th.accent2, 0.35 + (k % 2) * 0.15) }))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
