// SAFELIGHT — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-5.js, template "Safelight").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Syne / body Hanken Grotesk. Camera set ["zoomIn","pushL","pushD","zoomOut","pushR","hopU"] at stride 7/3.
// 41 scenes, 308s, 2 spine overrides.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "safelight",
  label: "Safelight",

  // ---- type ----
  display: "Syne", displayFallback: "system-ui, sans-serif",
  body: "Hanken Grotesk", bodyFallback: "system-ui, sans-serif",
  em: 0.7097, bodyEm: 0.6151,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#efe9df", ink: "#16110f", accent: t.accent || "#e2483d", accent2: t.accent2 || "#c9922c", sageT: "#1c1614", terraT: "#221614" }),
  accents: ["accent","accent2"],
  accentOpts: ["#e2483d","#c23d5e","#e2743d"],
  accent2Opts: ["#c9922c","#8c9e6b","#6b8c9e"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#100c0b",

  // ---- camera + motion ----
  cams: ["zoomIn","pushL","pushD","zoomOut","pushR","hopU"],
  camStride: 7, camOff: 3,
  ambient: 1.6,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["SAFELIGHT · A DARKROOM FILM","THIRTY-SIX FRAMES ONLY","NINE MINUTES THIRTY","NO PEEKING, EVER","GRAIN IS NOT NOISE"],
  foots: ["developed by hand under one red bulb","the negatives are the archive, not the files","agitate for ten seconds, then leave it","frame twelve was the one","nothing here can be undone"],
  sides: ["LOAD · SOUP · PRINT · DRY","THIRTY-SIX CHANCES, TOTAL","THE NEGATIVE REMEMBERS"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 41 scenes.
  spineOverrides: [["insert",36,"Patience"],[2,"Blink"]],

  strings: {
    brandName: "Safelight",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("circle", { cx: u.W - 180, cy: 200, r: 44 + Math.sin(t * 1.6) * 6, fill: rgba(th.accent, 0.85) }),
      R("circle", { cx: u.W - 180, cy: 200, r: 130 + Math.sin(t * 1.6) * 20, fill: rgba(th.accent, 0.1) }),
      R("line", { x1: 90, y1: 300, x2: u.W - 340, y2: 300, stroke: rgba(th.paper, 0.35), strokeWidth: 4 }),
      [0, 1, 2, 3].map((k) => {
        const sway = Math.sin(t * 0.9 + k * 1.2) * 5;
        return R("g", { key: k, transform: "translate(" + (170 + k * 200 + 20) + ",300) rotate(" + sway + ")" },
          R("rect", { x: -3, y: 0, width: 6, height: 30, fill: rgba(th.paper, 0.5) }),
          R("rect", { x: -40, y: 30, width: 80, height: 220 + k * 26, fill: rgba(th.paper, 0.14), stroke: rgba(th.paper, 0.35), strokeWidth: 2 }),
          Array.from({ length: 5 }).map((_, f) => R("rect", { key: f, x: -30, y: 46 + f * (40 + k * 5), width: 60, height: 30 + k * 3, fill: rgba(th.accent2, 0.16) })),
          Array.from({ length: 9 }).map((_, h) => R("rect", { key: "h" + h, x: -39, y: 36 + h * 26, width: 6, height: 11, fill: rgba(th.ink, 0.85) })));
      }),
      (function () {
        const rp = (t * 0.6) % 1;
        return R("g", { transform: "translate(300," + (u.H - 220) + ")" },
          R("rect", { x: -190, y: -26, width: 380, height: 86, rx: 12, fill: "none", stroke: rgba(th.paper, 0.4), strokeWidth: 5 }),
          R("ellipse", { rx: 50 + rp * 110, ry: 12 + rp * 26, cy: 18, fill: "none", stroke: rgba(th.accent, 0.5 * (1 - rp)), strokeWidth: 4 }),
          R("ellipse", { rx: 26 + rp * 70, ry: 6 + rp * 18, cy: 18, fill: "none", stroke: rgba(th.accent, 0.35 * (1 - rp)), strokeWidth: 3 }));
      })()),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
