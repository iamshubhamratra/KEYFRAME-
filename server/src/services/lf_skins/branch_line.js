// BRANCH LINE — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-5.js, template "BranchLine").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Staatliches / body Onest. Camera set ["pushL","pushR","zoomIn","hopU","zoomOut","drop"] at stride 3/2.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "branch-line",
  label: "Branch Line",

  // ---- type ----
  display: "Staatliches", displayFallback: "system-ui, sans-serif",
  body: "Onest", bodyFallback: "system-ui, sans-serif",
  em: 0.4388, bodyEm: 0.6405,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f0ecdd", ink: "#1d2420", accent: t.accent || "#a83a2c", accent2: t.accent2 || "#3f6b5f", sageT: "#e4ece4", terraT: "#f6e2d6" }),
  accents: ["accent","accent2"],
  accentOpts: ["#a83a2c","#2c5f8c","#8c6b2c"],
  accent2Opts: ["#3f6b5f","#5f5f7a","#6b7a3f"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#1a1f1c",

  // ---- camera + motion ----
  cams: ["pushL","pushR","zoomIn","hopU","zoomOut","drop"],
  camStride: 3, camOff: 2,
  ambient: 1.6,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["BRANCH LINE · A RAILWAY FILM","PRESERVED, NOT RESTORED","THE 10:42 RUNS DAILY","COAL BY THE SHOVEL","VOLUNTEERS ONLY"],
  foots: ["the loco is older than the line","signalled by hand, from a wooden box","coal shovelled by volunteers, all of it","eleven miles, two tunnels, one bridge","the 10:42 has never once been late"],
  sides: ["FIRE · STEAM · SIGNAL · GO","ELEVEN MILES OF TRACK","SIGNALLED ENTIRELY BY HAND"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Blink"]],

  strings: {
    brandName: "Branch Line",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("rect", { x: -20, y: u.H - 190, width: u.W + 40, height: 16, fill: rgba(th.ink, 0.5) }),
      R("rect", { x: -20, y: u.H - 150, width: u.W + 40, height: 16, fill: rgba(th.ink, 0.5) }),
      Array.from({ length: 26 }).map((_, k) => R("rect", { key: "sl" + k, x: ((k * 90 - t * 200) % (u.W + 180) + (u.W + 180)) % (u.W + 180) - 90, y: u.H - 196, width: 40, height: 62, fill: rgba("#6b5330", 0.4) })),
      (function () {
        const lx = ((t * 150) % (u.W + 700)) - 350;
        return R("g", { transform: "translate(" + lx + "," + (u.H - 200) + ")" },
          R("rect", { x: -30, y: -110, width: 210, height: 76, rx: 8, fill: rgba(th.accent, 0.9) }),
          R("rect", { x: 150, y: -170, width: 90, height: 136, rx: 8, fill: rgba(th.ink, 0.85) }),
          R("rect", { x: -46, y: -142, width: 34, height: 40, rx: 6, fill: rgba(th.ink, 0.85) }),
          R("circle", { cx: 10, cy: -12, r: 30, fill: rgba(th.ink, 0.9) }),
          R("circle", { cx: 100, cy: -12, r: 30, fill: rgba(th.ink, 0.9) }),
          R("circle", { cx: 190, cy: -20, r: 22, fill: rgba(th.ink, 0.9) }),
          [0, 1, 2, 3].map((k) => {
            const ph = (t * 0.6 + k * 0.25) % 1;
            return R("circle", { key: k, cx: -30 + ph * 40, cy: -150 - ph * 220, r: 20 + ph * 40, fill: rgba(th.paper, 0.28 * (1 - ph)) });
          }));
      })(),
      (function () {
        const up = Math.sin(t * 0.7) > 0;
        return R("g", { transform: "translate(" + (u.W - 220) + "," + (u.H - 480) + ")" },
          R("rect", { x: -7, y: 0, width: 14, height: 290, fill: rgba(th.ink, 0.7) }),
          R("g", { transform: "rotate(" + (up ? -32 : 0) + " 0 20)" },
            R("rect", { x: 0, y: 8, width: 96, height: 22, rx: 4, fill: up ? rgba(th.accent2, 0.95) : rgba(th.accent, 0.95) }),
            R("circle", { cx: 76, cy: 19, r: 7, fill: rgba(th.paper, 0.9) })));
      })(),
      Array.from({ length: 5 }).map((_, k) => {
        const x = ((k * 420 - t * 200) % (u.W + 420) + (u.W + 420)) % (u.W + 420) - 210;
        return R("g", { key: "tp" + k },
          R("rect", { x: x, y: u.H - 560, width: 10, height: 370, fill: rgba(th.ink, 0.35) }),
          R("rect", { x: x - 30, y: u.H - 560, width: 70, height: 8, fill: rgba(th.ink, 0.35) }));
      })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
