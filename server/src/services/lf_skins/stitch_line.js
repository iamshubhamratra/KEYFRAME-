// STITCH LINE — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-4.js, template "StitchLine").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Gilda Display / body Albert Sans. Camera set ["pushL","zoomIn","pushD","pushR","hopU","zoomOut"] at stride 3/1.
// 40 scenes, 300.5s, the canonical spine unmodified.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "stitch-line",
  label: "Stitch Line",

  // ---- type ----
  display: "Gilda Display", displayFallback: "Georgia, serif",
  body: "Albert Sans", bodyFallback: "system-ui, sans-serif",
  em: 0.6588, bodyEm: 0.6398,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f7f0ea", ink: "#2b2129", accent: t.accent || "#c44a6b", accent2: t.accent2 || "#4d7a8c", sageT: "#e8eef0", terraT: "#f8e2e6" }),
  accents: ["accent","accent2"],
  accentOpts: ["#c44a6b","#a8446b","#c47a4a"],
  accent2Opts: ["#4d7a8c","#5f7a5f","#7a5f8c"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#2a2028",

  // ---- camera + motion ----
  cams: ["pushL","zoomIn","pushD","pushR","hopU","zoomOut"],
  camStride: 3, camOff: 1,
  ambient: 1.6,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["STITCH LINE · A MAKING FILM","MEASURE TWICE, ALWAYS","SEAM ALLOWANCE 1.5 CM","MADE TO FIT ONE BODY","UNPICKING IS PART OF IT"],
  foots: ["hemmed by hand, on the second attempt","the pattern was traced, not cut","seam ripper used with dignity","fabric bought two years ago, finally","it fits, because it was measured"],
  sides: ["MEASURE · CUT · SEW · PRESS","PRESS EVERY SINGLE SEAM","MADE FOR ONE BODY ONLY"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [],

  strings: {
    brandName: "Stitch Line",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      (function () {
        const bob = Math.abs(Math.sin(t * 7)) * 26, feed = ((t * 120) % 700);
        return R("g", { transform: "translate(" + (u.W - 420) + "," + (u.H - 380) + ")" },
          R("rect", { x: -40, y: -260, width: 80, height: 200, rx: 14, fill: rgba(th.ink, 0.75) }),
          R("rect", { x: -160, y: 40, width: 420, height: 40, rx: 10, fill: rgba(th.ink, 0.55) }),
          R("line", { x1: 0, y1: -60, x2: 0, y2: -10 + bob, stroke: rgba(th.paper, 0.9), strokeWidth: 6 }),
          R("path", { d: "M -160 20 h 420", stroke: rgba(th.accent, 0.4), strokeWidth: 3, strokeDasharray: "14 10", strokeDashoffset: -feed }),
          R("circle", { cx: 0, cy: -8 + bob, r: 5, fill: th.accent }));
      })(),
      R("path", { d: "M -20 " + (u.H - 250) + " h " + (u.W + 40), stroke: rgba(th.accent, 0.5), strokeWidth: 4, strokeDasharray: "18 12", strokeDashoffset: -((t * 100) % 300) }),
      [[170, 260, 0], [420, 220, 1], [200, 560, 2]].map(([x, y, i]) => R("g", { key: "pt" + i, transform: "translate(" + x + "," + y + ") rotate(" + (Math.sin(t * 0.6 + i) * 3 + (i - 1) * 6) + ")" },
        R("path", { d: i === 0 ? "M 0 0 L 210 -30 L 240 200 L 20 220 Z" : i === 1 ? "M 0 0 L 180 10 L 150 180 L 10 160 Z" : "M 0 0 L 160 -20 L 190 140 L 20 160 Z", fill: rgba(th.paper, u.dark ? 0.12 : 0.7), stroke: rgba(th.ink, 0.3), strokeWidth: 3, strokeDasharray: "10 8" }),
        R("circle", { cx: 40, cy: 40, r: 7, fill: "none", stroke: rgba(th.ink, 0.4), strokeWidth: 3 }))),
      R("g", { transform: "translate(120," + (u.H - 150) + ") rotate(-8)" },
        R("rect", { x: 0, y: 0, width: 620, height: 34, rx: 6, fill: rgba(th.accent2, 0.5) }),
        Array.from({ length: 13 }).map((_, k) => R("line", { key: k, x1: 20 + k * 46, y1: 0, x2: 20 + k * 46, y2: k % 2 ? 12 : 20, stroke: rgba(th.paper, 0.8), strokeWidth: 3 }))),
      Array.from({ length: 6 }).map((_, k) => R("circle", { key: "pin" + k, cx: 200 + k * 200 + Math.sin(t + k) * 8, cy: 180 + (k % 2) * 40, r: 6, fill: rgba(th.accent, 0.5) }))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
