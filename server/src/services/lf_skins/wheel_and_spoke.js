// WHEEL & SPOKE — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-3.js, template "WheelSpoke").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Anton / body Asap. Camera set ["pushL","pushR","spin","zoomIn","hopU","pushU"] at stride 7/3.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "wheel-and-spoke",
  label: "Wheel & Spoke",

  // ---- type ----
  display: "Anton", displayFallback: "system-ui, sans-serif",
  body: "Asap", bodyFallback: "system-ui, sans-serif",
  em: 0.4747, bodyEm: 0.5919,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f0f2f4", ink: "#171c22", accent: t.accent || "#f2c53d", accent2: t.accent2 || "#2c6be0", sageT: "#e6eef2", terraT: "#f7ecd6" }),
  accents: ["accent","accent2"],
  accentOpts: ["#f2c53d","#f26b3d","#3df2a0"],
  accent2Opts: ["#2c6be0","#7a2ce0","#2ca0e0"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#12161c",

  // ---- camera + motion ----
  cams: ["pushL","pushR","spin","zoomIn","hopU","pushU"],
  camStride: 7, camOff: 3,
  ambient: 2,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["WHEEL & SPOKE · A CYCLING FILM","TWELVE PERCENT AVERAGE","COFFEE AT THE SUMMIT","TYRES AT SIX BAR","NOBODY GETS DROPPED"],
  foots: ["filmed from the back of the group","one puncture, fixed in four minutes","the climb is worse than it looks","chain waxed, not oiled","the summit cafe closes at two"],
  sides: ["SPIN · CLIMB · DESCEND","SOFT PEDAL THE FIRST HOUR","EVERYONE WAITS AT THE TOP"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Rain"]],

  strings: {
    brandName: "Wheel & Spoke",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("path", { d: "M -20 " + (u.H - 170) + " L " + u.W + " " + (u.H - 170), stroke: rgba(th.paper, 0.25), strokeWidth: 110 }),
      Array.from({ length: 7 }).map((_, k) => R("rect", { key: k, x: ((k * 300 - t * 700) % (u.W + 300) + (u.W + 300)) % (u.W + 300) - 150, y: u.H - 176, width: 120, height: 12, rx: 6, fill: rgba(th.accent, 0.75) })),
      (function () {
        const wx = 280, wy = u.H - 250;
        return R("g", { transform: "translate(" + wx + "," + wy + ")" },
          R("circle", { r: 96, fill: "none", stroke: th.paper, strokeWidth: 10 }),
          Array.from({ length: 8 }).map((_, k) => R("line", { key: k, x1: 0, y1: 0, x2: Math.cos(t * 6 + k * Math.PI / 4) * 88, y2: Math.sin(t * 6 + k * Math.PI / 4) * 88, stroke: rgba(th.paper, 0.7), strokeWidth: 5 })),
          R("circle", { r: 16, fill: th.accent2 }),
          R("circle", { cx: 210, r: 96, fill: "none", stroke: th.paper, strokeWidth: 10 }),
          Array.from({ length: 8 }).map((_, k) => R("line", { key: "b" + k, x1: 210, y1: 0, x2: 210 + Math.cos(t * 6 + k * Math.PI / 4) * 88, y2: Math.sin(t * 6 + k * Math.PI / 4) * 88, stroke: rgba(th.paper, 0.7), strokeWidth: 5 })),
          R("path", { d: "M 0 0 L 90 -70 L 210 0 M 90 -70 L 150 -76", fill: "none", stroke: th.accent, strokeWidth: 12, strokeLinecap: "round" }));
      })(),
      (function () {
        const drw = (t * 0.16) % 1.2;
        return R("g", { transform: "translate(" + (u.W - 640) + ",300)" },
          R("path", { d: "M 0 200 L 120 150 L 240 170 L 360 60 L 480 100 L 560 0", fill: "none", stroke: rgba(th.accent2, 0.7), strokeWidth: 8, strokeDasharray: 900, strokeDashoffset: 900 * (1 - Math.min(1, drw)) }),
          R("text", { x: 470, y: -20, fontFamily: '"Asap", sans-serif', fontSize: 30, fill: rgba(th.paper, 0.6) }, "12%"));
      })(),
      Array.from({ length: 3 }).map((_, k) => R("line", { key: "s" + k, x1: 520 + ((t * 800 + k * 200) % 500), y1: u.H - 320 + k * 40, x2: 640 + ((t * 800 + k * 200) % 500), y2: u.H - 320 + k * 40, stroke: rgba(th.accent, 0.4 - k * 0.1), strokeWidth: 7 - k, strokeLinecap: "round" }))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
