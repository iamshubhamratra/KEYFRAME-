// LUNCH HOUR — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-1.js, template "LunchHour").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Archivo Black / body Archivo. Camera set ["pushR","zoomIn","pushU","pushL","spin","drop"] at stride 7/1.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "lunch-hour",
  label: "Lunch Hour",

  // ---- type ----
  display: "Archivo Black", displayFallback: "system-ui, sans-serif",
  body: "Archivo", bodyFallback: "system-ui, sans-serif",
  em: 0.7408, bodyEm: 0.6506,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#f2f1ec", ink: "#1d2126", accent: t.accent || "#f26b3c", accent2: t.accent2 || "#2c7a6b", sageT: "#e4ecea", terraT: "#f7e4d8" }),
  accents: ["accent","accent2"],
  accentOpts: ["#f26b3c","#e8483c","#f2a03c"],
  accent2Opts: ["#2c7a6b","#3c5f9e","#6b7a2c"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#171a1e",

  // ---- camera + motion ----
  cams: ["pushR","zoomIn","pushU","pushL","spin","drop"],
  camStride: 7, camOff: 1,
  ambient: 1.9,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["LUNCH HOUR · 12:00–13:00","SIXTY MINUTES, USED WELL","DESK LUNCH ABOLISHED","THE QUEUE IS THE PROBLEM","PACKED, NOT PURCHASED"],
  foots: ["timed with an actual stopwatch","the microwave queue is real","eaten away from the keyboard","prep happened on sunday","leftovers are a strategy"],
  sides: ["PACK · WALK · EAT","AWAY FROM THE SCREEN","TWELVE TO ONE, DEFENDED"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Rain"]],

  strings: {
    brandName: "Lunch Hour",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("g", { transform: "translate(" + (u.W - 240) + ",260)" },
        R("circle", { r: 150, fill: "none", stroke: rgba(th.ink, u.dark ? 0.18 : 0.22), strokeWidth: 14 }),
        Array.from({ length: 12 }).map((_, k) => R("line", { key: k, x1: 0, y1: -128, x2: 0, y2: -108, stroke: rgba(th.ink, 0.3), strokeWidth: k % 3 === 0 ? 8 : 4, transform: "rotate(" + k * 30 + ")" })),
        R("line", { x1: 0, y1: 0, x2: Math.cos(t * 2.2 - 1.57) * 120, y2: Math.sin(t * 2.2 - 1.57) * 120, stroke: th.accent, strokeWidth: 7, strokeLinecap: "round" }),
        R("line", { x1: 0, y1: 0, x2: Math.cos(t * 0.36 - 1.57) * 84, y2: Math.sin(t * 0.36 - 1.57) * 84, stroke: rgba(th.ink, 0.6), strokeWidth: 10, strokeLinecap: "round" }),
        R("circle", { r: 12, fill: th.accent })),
      (function () {
        const cells = [[0, 0, th.accent], [1, 0, th.accent2], [0, 1, "#e8c33c"], [1, 1, th.accent2], [2, 0, "#7a5f9e"], [2, 1, th.accent]];
        return R("g", { transform: "translate(160," + (u.H - 420) + ")" },
          R("rect", { x: -16, y: -16, width: 372, height: 258, rx: 22, fill: rgba(th.ink, 0.14), stroke: rgba(th.ink, 0.3), strokeWidth: 5 }),
          cells.map(([cx, cy, c], k) => {
            const e = ((t * 0.5 + k * 0.16) % 1) < 0.72 ? 1 : 0.35;
            return R("rect", { key: k, x: cx * 118, y: cy * 118, width: 106, height: 106, rx: 16, fill: rgba(c, 0.75 * e), transform: "rotate(" + Math.sin(t * 1.2 + k) * 1.6 + " " + (cx * 118 + 53) + " " + (cy * 118 + 53) + ")" });
          }));
      })(),
      Array.from({ length: 9 }).map((_, k) => {
        const q = ((t * 0.3 + k * 0.11) % 1);
        return R("circle", { key: "q" + k, cx: 120 + q * (u.W - 240), cy: u.H - 140, r: 13, fill: rgba(th.accent2, 0.5 - q * 0.3) });
      }),
      R("path", { d: "M 60 200 q 200 60 420 -20", fill: "none", stroke: rgba(th.accent, 0.18), strokeWidth: 40, strokeLinecap: "round" })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
