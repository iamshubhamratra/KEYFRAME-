// NIGHT SHIFT — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-3.js, template "NightShift").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Unbounded / body Wix Madefor Text. Camera set ["pushR","zoomIn","pushU","pushL","drop","zoomOut"] at stride 5/1.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "night-shift",
  label: "Night Shift",

  // ---- type ----
  display: "Unbounded", displayFallback: "system-ui, sans-serif",
  body: "Wix Madefor Text", bodyFallback: "system-ui, sans-serif",
  em: 0.8517, bodyEm: 0.6642,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#e8e9f0", ink: "#0d1018", accent: t.accent || "#f2b03c", accent2: t.accent2 || "#8c6aff", sageT: "#141c22", terraT: "#1c1620" }),
  accents: ["accent","accent2"],
  accentOpts: ["#f2b03c","#3ce0c4","#ff6b8a"],
  accent2Opts: ["#8c6aff","#3c8cff","#ff8c3c"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#0b0d14",

  // ---- camera + motion ----
  cams: ["pushR","zoomIn","pushU","pushL","drop","zoomOut"],
  camStride: 5, camOff: 1,
  ambient: 1.6,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["NIGHT SHIFT · A CITY FILM","02:00 TO 10:00","THE OVENS NEVER COOL","NOBODY ELSE IS AWAKE","FIRST TRAY AT FIVE"],
  foots: ["filmed between two and six","the city is different at that hour","coffee number four, no regrets","the first customer arrives at 06:40","sunrise is a shift change"],
  sides: ["MIX · PROVE · BAKE · OPEN","AWAKE WHILE YOU SLEEP","FIRST TRAY AT FIVE AM"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Blink"]],

  strings: {
    brandName: "Night Shift",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      Array.from({ length: 6 }).map((_, k) => {
        const x = 60 + k * 300, h = 320 + ((k * 97) % 260);
        return R("g", { key: "b" + k },
          R("rect", { x: x, y: u.H - h, width: 200, height: h, fill: rgba(th.ink, 0.9) }),
          Array.from({ length: 12 }).map((_, w) => {
            const on = Math.sin(t * 0.6 + k * 3 + w * 1.7) > 0.4;
            return R("rect", { key: w, x: x + 24 + (w % 3) * 58, y: u.H - h + 30 + Math.floor(w / 3) * 62, width: 40, height: 40, rx: 4, fill: rgba(on ? th.accent : th.paper, on ? 0.75 : 0.07) });
          }));
      }),
      (function () {
        const vx = ((t * 140) % (u.W + 400)) - 200;
        return R("g", { transform: "translate(" + vx + "," + (u.H - 120) + ")" },
          R("rect", { x: -90, y: -70, width: 180, height: 60, rx: 10, fill: rgba(th.accent2, 0.85) }),
          R("rect", { x: 60, y: -50, width: 50, height: 40, rx: 8, fill: rgba(th.accent2, 0.85) }),
          R("circle", { cx: -50, r: 18, fill: rgba(th.ink, 0.95) }),
          R("circle", { cx: 60, r: 18, fill: rgba(th.ink, 0.95) }),
          R("circle", { cx: 116, cy: -32, r: 8, fill: rgba(th.accent, 0.9) }));
      })(),
      R("g", { transform: "translate(" + (u.W - 200) + ",230)" },
        R("circle", { r: 110, fill: "none", stroke: rgba(th.paper, 0.22), strokeWidth: 10 }),
        R("line", { x1: 0, y1: 0, x2: Math.cos(t * 2 - 1.57) * 88, y2: Math.sin(t * 2 - 1.57) * 88, stroke: th.accent, strokeWidth: 6, strokeLinecap: "round" }),
        R("line", { x1: 0, y1: 0, x2: Math.cos(t * 0.33 - 1.57) * 60, y2: Math.sin(t * 0.33 - 1.57) * 60, stroke: rgba(th.paper, 0.6), strokeWidth: 9, strokeLinecap: "round" }),
        R("circle", { r: 10, fill: th.accent2 })),
      Array.from({ length: 10 }).map((_, k) => R("circle", { key: "st" + k, cx: (k * 191 + 60) % u.W, cy: 90 + ((k * 53) % 160), r: 2, fill: rgba(th.paper, 0.2 + 0.4 * Math.abs(Math.sin(t * 1.6 + k * 2))) }))),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
