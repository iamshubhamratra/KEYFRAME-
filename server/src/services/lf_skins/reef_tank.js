// REEF TANK — skin for the shared long-form stage (services/lf_stage.js).
//
// GENERATED by scripts/gen-lf-skins.js from the handoff source
// (templete-design/longform-handoff/engine/lf-pack-5.js, template "ReefTank").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Outfit / body Nunito Sans. Camera set ["zoomOut","pushU","drop","pushR","zoomIn","pushL"] at stride 5/1.
// 40 scenes, 300.5s, 1 spine override.
//
// palette and World below are the AUTHORED functions, emitted verbatim. The World is
// re-evaluated per seek against an SVG-DOM shim whose entire surface is {R, rgba} plus the
// utils object — verified by evaluating all 30 authored Worlds against exactly that sandbox —
// so the backdrop is not a reinterpretation of the original, it IS the original.

const stage = require("../lf_stage");

const SKIN = {
  id: "reef-tank",
  label: "Reef Tank",

  // ---- type ----
  display: "Outfit", displayFallback: "system-ui, sans-serif",
  body: "Nunito Sans", bodyFallback: "system-ui, sans-serif",
  em: 0.629, bodyEm: 0.6441,

  // ---- palette ----
  // The authored function, called with {} for the design's own defaults. It returns the same
  // six roles on every one of the 27 films, so the stage can rely on them by name: paper and
  // ink are the type system, sageT and terraT are the alternating tinted grounds, and
  // accent/accent2 are the two a brand may take over at their authored luminance.
  palette: (t) => ({ paper: "#eef4f2", ink: "#0b1c22", accent: t.accent || "#ff8a5c", accent2: t.accent2 || "#3fd0c4", sageT: "#123038", terraT: "#1c2630" }),
  accents: ["accent","accent2"],
  accentOpts: ["#ff8a5c","#ffc45c","#ff5c8a"],
  accent2Opts: ["#3fd0c4","#5c9eff","#8adf5c"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  desk: "#08181e",

  // ---- camera + motion ----
  cams: ["zoomOut","pushU","drop","pushR","zoomIn","pushL"],
  camStride: 5, camOff: 1,
  ambient: 1.4,

  // ---- the garnish voice ----
  // Rotated by scene index so no two consecutive frames read the same. Authored copy.
  tags: ["REEF TANK · AN AQUARIUM FILM","PARAMETERS BEFORE PETS","NOTHING GOOD HAPPENS FAST","TEST WEEKLY, ALWAYS","PATIENCE IS THE HOBBY"],
  foots: ["nothing in this tank was rushed","salinity 1.025, held for two years","the clownfish pair chose each other","algae is a message, not an enemy","one water change a week, forever"],
  sides: ["TEST · DOSE · WAIT · WATCH","PARAMETERS BEFORE LIVESTOCK","TWO YEARS TO LOOK EASY"],

  // ---- the spine ----
  // Which renderer runs at which index. The stage owns the canonical 40-name order; this is
  // only what THIS film does differently. ["insert", i, name] splices, [i, name] substitutes.
  // Resolves to: 40 scenes.
  spineOverrides: [[2,"Wave"]],

  strings: {
    brandName: "Reef Tank",
  },

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, utils) — no Math.random, no
  // Date, no accumulated state anywhere in the six pack files — which is what lets it be
  // re-evaluated deterministically on every seek, forwards or backwards.
  World: (th, t, u) => R("g", null,
      R("rect", { x: 90, y: 180, width: u.W - 180, height: u.H - 340, rx: 10, fill: rgba(th.accent2, 0.08), stroke: rgba(th.paper, 0.25), strokeWidth: 6 }),
      R("rect", { x: 90, y: u.H - 200, width: u.W - 180, height: 40, fill: rgba("#e8d8b0", 0.35) }),
      (function () {
        const sx = ((t * 110) % (u.W + 500)) - 250;
        return R("g", null, Array.from({ length: 7 }).map((_, k) => {
          const ox = (k % 4) * 70, oy = Math.floor(k / 4) * 60 + Math.sin(t * 3 + k) * 10;
          return R("g", { key: k, transform: "translate(" + (sx + ox) + "," + (420 + oy) + ")" },
            R("ellipse", { rx: 26, ry: 14, fill: rgba(k % 3 === 0 ? th.accent : th.accent2, 0.85) }),
            R("polygon", { points: "-24,0 -44,-12 -44,12", fill: rgba(k % 3 === 0 ? th.accent : th.accent2, 0.85) }),
            R("circle", { cx: 13, cy: -3, r: 3, fill: rgba(th.ink, 0.9) }));
        }));
      })(),
      [0, 1].map((k) => R("g", { key: "an" + k, transform: "translate(" + (300 + k * (u.W - 700)) + "," + (u.H - 220) + ")" },
        Array.from({ length: 11 }).map((_, j) => {
          const a = (j / 11) * Math.PI * 2, w = Math.sin(t * 2.4 + j + k) * 10;
          return R("line", { key: j, x1: 0, y1: 0, x2: Math.cos(a) * (40 + w), y2: -Math.abs(Math.sin(a) * (36 + w)) - 10, stroke: rgba(k ? th.accent : th.accent2, 0.7), strokeWidth: 7, strokeLinecap: "round" });
        }),
        R("circle", { r: 14, fill: rgba(th.ink, 0.5) }))),
      Array.from({ length: 9 }).map((_, k) => {
        const ph = (t * (0.22 + (k % 3) * 0.08) + k * 0.11) % 1;
        return R("circle", { key: "b" + k, cx: 200 + ((k * 173) % (u.W - 400)) + Math.sin(t * 1.4 + k) * 14, cy: (u.H - 200) - ph * (u.H - 400), r: 4 + (k % 3) * 3, fill: "none", stroke: rgba(th.paper, 0.3 * (1 - ph) + 0.08), strokeWidth: 3 });
      }),
      R("rect", { x: 90, y: 180, width: u.W - 180, height: 26, fill: rgba(th.paper, 0.12) })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
