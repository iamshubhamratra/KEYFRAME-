// SCHEMATIC BLUE — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        schematic-blue
//   renderer    film-schematic-blue
//   stage       landscape (1920x1080)
//   display     IBM Plex Mono / body IBM Plex Sans
//   ground      #eef2f7 (sheet), light
//   world       grid, grid, arcs, arcs

const stage = require("../film_stage");

const SKIN = {
  id: "schematic-blue",
  label: "Schematic Blue",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "landscape",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.3,"h":0.34},"montage":{"h":0.3},"statement":{"h":0.3}},


  // ---- type ----
  display: "IBM Plex Mono", displayFallback: "ui-monospace, Menlo, monospace",
  body: "IBM Plex Sans", bodyFallback: "system-ui, sans-serif",
  mono: "IBM Plex Mono",
  em: 0.6,
  titleLine: 1.14, titleSpace: "-0.005em",
  titlePreset: "streak", itemPreset: "streak",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ sheet: t.sheet || "#eef2f7", mist: t.mist || "#dbe4ef", ink: t.ink || "#101b28", steel: t.steel || "#44607e", blue: t.blue || "#14509b", cyan: t.cyan || "#4fb8d8", rust: t.rust || "#a8451a" }),
  accents: ["blue","steel","rust"],
  groundKey: "sheet", inkKey: "ink", paperKey: "sheet",
  dark: false,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "sheet",
      "fg": "ink",
      "hi": "blue",
      "world": true,
      "top": 300,
      "size": 104,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "outline",
        "c": "blue"
      }
    },
    "statement": {
      "bg": "mist",
      "fg": "ink",
      "hi": "blue",
      "world": false,
      "top": 250,
      "size": 116,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "sheet",
        "r": 6,
        "glow": "steel"
      }
    },
    "feature": {
      "bg": "sheet",
      "fg": "ink",
      "hi": "blue",
      "world": true,
      "top": 190,
      "size": 88,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "mist",
        "r": 4,
        "glow": "blue"
      },
      "chips": {
        "v": "outline",
        "colors": [
          "blue",
          "steel",
          "rust"
        ],
        "text": "ink"
      }
    },
    "montage": {
      "bg": "mist",
      "fg": "ink",
      "hi": "blue",
      "world": false,
      "top": 170,
      "size": 84,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "sheet",
        "line": "steel",
        "label": "ink",
        "r": 4,
        "labelSize": 26
      }
    },
    "stats": {
      "bg": "blue",
      "fg": "sheet",
      "hi": "cyan",
      "world": true,
      "top": 240,
      "size": 92,
      "upper": false,
      "align": "center",
      "cols": [
        "cyan",
        "sheet",
        "cyan"
      ],
      "num": 236
    },
    "cta": {
      "bg": "sheet",
      "fg": "ink",
      "hi": "rust",
      "world": true,
      "top": 300,
      "size": 100,
      "upper": false,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "blue",
        "c": "sheet"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "mist",
      "fg": "ink",
      "hi": "blue",
      "world": false,
      "top": 210,
      "size": 84,
      "upper": false,
      "align": "left",
      "cardBg": "sheet",
      "line": "steel"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["spin","pushR","pushL","spin","pushD","zoomIn"], camMul: 1, camOff: 3,
  mag: {"x":0.34,"y":0.2,"rot":0.24,"skew":0,"zin":0.14,"zout":0.12,"driftX":3,"driftY":2,"driftZ":0.015,"slide":0.1,"inn":0.2,"out":0.72},
  ambient: 0.5,
  energy: 0.55,
  badge: "outline",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"feature":"Typing","montage":"Scroll"},

  strings: {"brandName":"Schematic Blue"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M12 5 L12 19 M5 12 L19 12"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "linear-gradient(160deg, " + theme.sheet + " 0%, " + bg + " 58%, " + theme.mist + " 100%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",null,Array.from({length:24}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/24,y1:0,x2:W*i/24,y2:H,stroke:rgba(theme.steel,0.16),strokeWidth:W*0});}).concat(Array.from({length:41}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/41,x2:W,y2:H*i/41,stroke:rgba(theme.steel,0.11),strokeWidth:W*0});}))),
    R("g",null,Array.from({length:6}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/6,y1:0,x2:W*i/6,y2:H,stroke:rgba(theme.blue,0.1),strokeWidth:W*0});}).concat(Array.from({length:10}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/10,x2:W,y2:H*i/10,stroke:rgba(theme.blue,0.07),strokeWidth:W*0});}))),
    R("g",null,Array.from({length:3}).map(function(_,i){
      var rr=W*0.42*(0.5+i*0.18), a0=t*0.13+i*1.1, a1=a0+1.7;
      var x0=W*0.74+Math.cos(a0)*rr, y0=H*0.44+Math.sin(a0)*rr;
      var x1=W*0.74+Math.cos(a1)*rr, y1=H*0.44+Math.sin(a1)*rr;
      return R("path",{key:i,d:"M"+x0+" "+y0+" A"+rr+" "+rr+" 0 0 1 "+x1+" "+y1,stroke:rgba(theme.blue,0.14),strokeWidth:W*0,fill:"none",strokeLinecap:"round"});
    })),
    R("g",null,Array.from({length:2}).map(function(_,i){
      var rr=W*0.26*(0.5+i*0.18), a0=t*0.09+i*1.1, a1=a0+1.7;
      var x0=W*0.2+Math.cos(a0)*rr, y0=H*0.7+Math.sin(a0)*rr;
      var x1=W*0.2+Math.cos(a1)*rr, y1=H*0.7+Math.sin(a1)*rr;
      return R("path",{key:i,d:"M"+x0+" "+y0+" A"+rr+" "+rr+" 0 0 1 "+x1+" "+y1,stroke:rgba(theme.steel,0.12),strokeWidth:W*0,fill:"none",strokeLinecap:"round"});
    }))
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
