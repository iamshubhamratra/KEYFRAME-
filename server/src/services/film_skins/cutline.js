// CUTLINE — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        cutline
//   renderer    film-cutline
//   stage       landscape (1920x1080)
//   display     Archivo Black / body JetBrains Mono
//   ground      #0c0e14 (bgDark), dark
//   world       stripes, grid

const stage = require("../film_stage");

const SKIN = {
  id: "cutline",
  label: "Cutline",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "landscape",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.48,"h":0.52},"montage":{"h":0.42},"statement":{"h":0.48}},


  // ---- type ----
  display: "Archivo Black", displayFallback: "Impact, sans-serif",
  body: "JetBrains Mono", bodyFallback: "monospace",
  mono: "JetBrains Mono",
  em: 0.52,
  titleLine: 0.98, titleSpace: "-0.03em",
  titlePreset: "stamp", itemPreset: "pop",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ bgDark: t.bgDark || "#0c0e14", panelBlue: t.panelBlue || "#1a56ff", panelAmber: t.panelAmber || "#ff4500", accentYellow: t.accentYellow || "#ffd028", paper: t.paper || "#f4f6fa", ink: t.ink || "#080a0f" }),
  accents: ["accentYellow","panelBlue","panelAmber"],
  groundKey: "bgDark", inkKey: "ink", paperKey: "paper",
  dark: true,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "bgDark",
      "fg": "paper",
      "hi": "accentYellow",
      "world": true,
      "top": 220,
      "size": 110,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "pill",
        "c": "accentYellow"
      }
    },
    "statement": {
      "bg": "panelBlue",
      "fg": "paper",
      "hi": "accentYellow",
      "world": false,
      "top": 200,
      "size": 120,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "ink",
        "r": 28
      }
    },
    "feature": {
      "bg": "paper",
      "fg": "ink",
      "hi": "panelBlue",
      "world": false,
      "top": 180,
      "size": 96,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "bgDark",
        "r": 4
      },
      "chips": {
        "v": "square",
        "colors": [
          "panelBlue",
          "panelAmber"
        ],
        "text": "paper"
      }
    },
    "montage": {
      "bg": "bgDark",
      "fg": "paper",
      "hi": "panelAmber",
      "world": true,
      "top": 180,
      "size": 92,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "panelBlue",
        "line": "accentYellow",
        "label": "paper",
        "r": 0,
        "labelSize": 24
      }
    },
    "stats": {
      "bg": "panelAmber",
      "fg": "ink",
      "hi": "ink",
      "world": false,
      "top": 220,
      "size": 100,
      "upper": false,
      "align": "left",
      "cols": [
        "accentYellow",
        "ink",
        "bgDark"
      ],
      "num": 160
    },
    "cta": {
      "bg": "bgDark",
      "fg": "paper",
      "hi": "accentYellow",
      "world": true,
      "top": 240,
      "size": 110,
      "upper": false,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "accentYellow",
        "c": "ink"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "panelBlue",
      "fg": "paper",
      "hi": "accentYellow",
      "world": false,
      "top": 200,
      "size": 96,
      "upper": false,
      "align": "left",
      "cardBg": "ink",
      "line": "accentYellow"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["drop","pushL","pushR","drop","pushD","zoomOut"], camMul: 3, camOff: 2,
  mag: {"x":0.8,"y":0.4,"rot":0,"skew":0,"zin":0.2,"zout":0.2,"driftX":0,"driftY":0,"driftZ":0,"slide":0.4,"inn":0.1,"out":0.9},
  ambient: 1.1,
  energy: 1.2,
  badge: "square",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"statement":"Toggle","feature":"Code","montage":"Swipe"},

  strings: {"brandName":"Cutline"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: theme.currentBg, stroke: "none" },
    R2("path",{d:"M13 3 L6 13 L11 13 L10 21 L18 10 L13 10 Z"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "linear-gradient(45deg, " + theme.bgDark + " 0%, " + bg + " 58%, " + theme.panelBlue + " 100%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",{transform:"rotate(45 "+(W/2)+" "+(H/2)+")"},Array.from({length:12}).map(function(_,i){return R("rect",{key:i,x:-W*0.5+ (W*2*i/12) + Math.sin(t*0.54)*W*0.02,y:-H*0.5,width:W*0.08,height:H*2,fill:rgba(theme.panelBlue,0.15)});})),
    R("g",null,Array.from({length:8}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/8,y1:0,x2:W*i/8,y2:H,stroke:rgba(theme.paper,0.06),strokeWidth:W*0.02});}).concat(Array.from({length:14}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/14,x2:W,y2:H*i/14,stroke:rgba(theme.paper,0.04),strokeWidth:W*0.02});})))
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
