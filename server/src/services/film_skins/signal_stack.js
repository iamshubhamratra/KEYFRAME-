// SIGNAL STACK — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        signal-stack
//   renderer    film-signal-stack
//   stage       landscape (1920x1080)
//   display     Anton / body Barlow Semi Condensed
//   ground      #05070E (void), dark
//   world       beam, glow, glow

const stage = require("../film_stage");

const SKIN = {
  id: "signal-stack",
  label: "Signal Stack",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "landscape",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.58,"h":0.6},"montage":{"h":0.52},"statement":{"h":0.62}},


  // ---- type ----
  display: "Anton", displayFallback: "Impact, sans-serif",
  body: "Barlow Semi Condensed", bodyFallback: "system-ui, sans-serif",
  mono: "JetBrains Mono",
  em: 0.46,
  titleLine: 0.92, titleSpace: "-0.02em",
  titlePreset: "pop", itemPreset: "rise",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ void: t.void || "#05070E", carbon: t.carbon || "#121A2B", ink: t.ink || "#000208", paper: t.paper || "#F5F6F8", signal: t.signal || "#A3E635", azure: t.azure || "#4C82F0" }),
  accents: ["signal","azure","paper"],
  groundKey: "void", inkKey: "ink", paperKey: "paper",
  dark: true,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "void",
      "fg": "paper",
      "hi": "signal",
      "world": true,
      "top": 400,
      "size": 118,
      "upper": true,
      "align": "left",
      "kicker": {
        "v": "pill",
        "c": "signal"
      }
    },
    "statement": {
      "bg": "signal",
      "fg": "ink",
      "hi": "void",
      "world": false,
      "top": 330,
      "size": 124,
      "upper": true,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "ink",
        "r": 0,
        "glow": "ink"
      }
    },
    "feature": {
      "bg": "void",
      "fg": "paper",
      "hi": "azure",
      "world": true,
      "top": 250,
      "size": 96,
      "upper": true,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "ink",
        "r": 0,
        "glow": "azure"
      },
      "chips": {
        "v": "outline",
        "colors": [
          "signal",
          "azure",
          "paper"
        ],
        "text": "paper"
      }
    },
    "montage": {
      "bg": "carbon",
      "fg": "paper",
      "hi": "signal",
      "world": false,
      "top": 210,
      "size": 92,
      "upper": true,
      "align": "left",
      "tile": {
        "bg": "ink",
        "line": "signal",
        "label": "paper",
        "r": 0,
        "labelSize": 28
      }
    },
    "stats": {
      "bg": "ink",
      "fg": "paper",
      "hi": "signal",
      "world": false,
      "top": 300,
      "size": 96,
      "upper": true,
      "align": "left",
      "cols": [
        "signal",
        "paper",
        "azure"
      ],
      "num": 150
    },
    "cta": {
      "bg": "void",
      "fg": "paper",
      "hi": "signal",
      "world": true,
      "top": 240,
      "size": 104,
      "upper": true,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "signal",
        "c": "ink"
      },
      "logoShape": "none"
    },
    "app": {
      "bg": "carbon",
      "fg": "paper",
      "hi": "azure",
      "world": false,
      "top": 300,
      "size": 92,
      "upper": true,
      "align": "left",
      "cardBg": "ink",
      "line": "signal"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["zoomIn","pushR","pushL","zoomOut","pushD","zoomIn"], camMul: 3, camOff: 1,
  mag: {"x":1.25,"y":0.5,"rot":0.35,"skew":0,"zin":0.55,"zout":0.46,"driftX":14,"driftY":6,"driftZ":0.06,"slide":0.3,"inn":0.2,"out":0.9},
  ambient: 1.2,
  energy: 1.35,
  badge: "square",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"feature":"Code","stats":"Ring"},

  strings: {"brandName":"Signal Stack"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: theme.currentBg, stroke: "none" },
    R2("path",{d:"M13 3 L6 13 L11 13 L10 21 L18 10 L13 10 Z"})),

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",{transform:"rotate("+(-22+Math.sin(t*0.16)*6)+" "+(W*0.5)+" "+(H*0.5)+")"},
      R("rect",{x:W*0.5-W*0.2/2,y:-H*0.2,width:W*0.2,height:H*1.5,fill:rgba(theme.paper,0.09)})),
    R("circle",{cx:W*0.18,cy:H*0.72,r:W*0.37*(1+0.05*Math.sin(t*0.6+0.8)),fill:rgba(theme.signal,0.01)}),
    R("circle",{cx:W*0.18,cy:H*0.72,r:W*0.3*(1+0.04*Math.sin(t*0.6+1.2)),fill:rgba(theme.signal,0.03)}),
    R("circle",{cx:W*0.18,cy:H*0.72,r:W*0.22*(1+0.04*Math.sin(t*0.6+1.6)),fill:rgba(theme.signal,0.04)}),
    R("circle",{cx:W*0.18,cy:H*0.72,r:W*0.15*(1+0.03*Math.sin(t*0.6+2)),fill:rgba(theme.signal,0.06)}),
    R("circle",{cx:W*0.18,cy:H*0.72,r:W*0.07*(1+0.02*Math.sin(t*0.6+2.4)),fill:rgba(theme.signal,0.13)}),
    R("circle",{cx:W*0.86,cy:H*0.24,r:W*0.31*(1+0.05*Math.sin(t*0.5+0.8)),fill:rgba(theme.azure,0.01)}),
    R("circle",{cx:W*0.86,cy:H*0.24,r:W*0.25*(1+0.04*Math.sin(t*0.5+1.2)),fill:rgba(theme.azure,0.02)}),
    R("circle",{cx:W*0.86,cy:H*0.24,r:W*0.19*(1+0.04*Math.sin(t*0.5+1.6)),fill:rgba(theme.azure,0.04)}),
    R("circle",{cx:W*0.86,cy:H*0.24,r:W*0.13*(1+0.03*Math.sin(t*0.5+2)),fill:rgba(theme.azure,0.05)}),
    R("circle",{cx:W*0.86,cy:H*0.24,r:W*0.06*(1+0.02*Math.sin(t*0.5+2.4)),fill:rgba(theme.azure,0.1)})
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
