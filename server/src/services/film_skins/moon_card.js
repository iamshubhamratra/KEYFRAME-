// MOON CARD — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by scripts/gen-film-skins.js from the handoff source
// (templete-design/keyframe-handoff/source/mega-pack-3.js, template "MoonCard").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Italiana / body Sen. Camera set ["zoomOut","drop","pushL","zoomIn","pushU","pushR"].
// Entrances: title "drowse", items "rise".
// Owns the Swipe:flip, Morph:fade, Ring:ring, Typing:caret mechanics.
//
// palette / icon / World below are the AUTHORED functions, emitted verbatim — the world in
// particular is re-evaluated per seek against an SVG-DOM shim, so the backdrop is not a
// reinterpretation of the original, it IS the original.

const stage = require("../film_stage");

const SKIN = {
  id: "moon-card",
  label: "Moon Card",

  // ---- type ----
  display: "Italiana", displayFallback: "serif",
  body: "Sen", bodyFallback: "sans-serif",
  mono: null,
  em: 0.42,
  titleLine: 1.12, titleSpace: "0.04em",
  titlePreset: "drowse", itemPreset: "rise",

  // ---- palette ----
  // The authored function. Called with {} to get the design's own defaults; the brand's
  // accents are then hue-mapped onto `accents` slot by slot at their authored luminance,
  // and every other stop rotates onto the brand's lead hue — so a brand recolours the whole
  // world, not just the headline.
  palette: (t) => ({ violet: t.violet || "#1d1430", gold: t.gold || "#d4af6a", blush: "#e8b4c8", bone: "#f2ead9", ink: "#140e22" }),
  accents: ["gold","blush","violet"],
  groundKey: "violet", inkKey: "ink", paperKey: "bone",
  dark: true,

  // ---- per-beat look (verbatim) ----
  look: {
    "hook": {
      "bg": "violet",
      "fg": "bone",
      "hi": "gold",
      "world": true,
      "top": 340,
      "size": 116,
      "upper": true,
      "kicker": {
        "v": "bare",
        "c": "gold"
      }
    },
    "statement": {
      "bg": "ink",
      "fg": "bone",
      "hi": "blush",
      "world": true,
      "top": 640,
      "size": 138,
      "upper": true
    },
    "feature": {
      "bg": "violet",
      "fg": "bone",
      "hi": "gold",
      "world": true,
      "top": 250,
      "size": 92,
      "upper": true,
      "card": {
        "v": "glow",
        "bg": "ink",
        "glow": "gold",
        "r": 20,
        "line": "bone"
      },
      "chips": {
        "v": "outline",
        "colors": [
          "gold",
          "blush"
        ],
        "text": "ink"
      }
    },
    "montage": {
      "bg": "ink",
      "fg": "bone",
      "hi": "gold",
      "world": true,
      "top": 258,
      "size": 90,
      "upper": true,
      "tile": {
        "bg": "#241a38",
        "line": "bone",
        "label": "gold",
        "r": 16,
        "glow": "gold"
      },
      "tilts": [
        -2,
        1.5,
        2,
        -1.5
      ]
    },
    "stats": {
      "bg": "violet",
      "fg": "bone",
      "hi": "gold",
      "world": true,
      "cols": [
        "gold",
        "blush",
        "bone"
      ],
      "num": 146,
      "glowNums": true,
      "upper": true
    },
    "cta": {
      "bg": "ink",
      "fg": "bone",
      "hi": "gold",
      "world": true,
      "top": 500,
      "size": 112,
      "upper": true,
      "btn": {
        "v": "glow",
        "bg": "gold",
        "c": "ink"
      },
      "logoShape": "circle"
    },
    "app": {
      "bg": "violet",
      "fg": "bone",
      "hi": "gold",
      "cardBg": "#281c40",
      "line": "bone",
      "world": true,
      "upper": true
    }
  },

  // ---- camera + motion ----
  cams: ["zoomOut","drop","pushL","zoomIn","pushU","pushR"], camMul: 5, camOff: 3,
  mag: {"rot":0.4,"driftY":9,"inn":0.3,"driftZ":0.035},
  ambient: 1.4,
  energy: 1,
  badge: "circle",
  

  // ---- the mechanics this pack owns ----
  // The handoff's promise is that no two templates share a mechanism. A pack that does not
  // declare a mechanic here never renders it.
  variants: {"Swipe":"flip","Morph":"fade","Ring":"ring","Typing":"caret"},

  strings: {
    brandName: "Moon Card",
  },

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: theme.currentBg }, R2("path", { d: "M15 3a9 9 0 1 0 6 15.5A10 10 0 0 1 15 3z" })),

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, progress, utils); no
  // Math.random and no Date, which is what lets it be re-evaluated deterministically on
  // every seek.
  World: (theme, t, p, u) => R("g", null,
      [0, 1, 2, 3, 4].map((i) => {
        const r = 26;
        return R("g", { key: i, transform: "translate(" + (u.W / 2 - 240 + i * 120) + ",180)" },
          R("circle", { r, fill: i === 4 ? theme.gold : "none", stroke: theme.gold, strokeWidth: 3, opacity: 0.85 }),
          i < 4 && R("path", { d: "M 0 -" + r + " a " + r + " " + r + " 0 0 1 0 " + (2 * r) + " a " + (r * (1 - i * 0.5)) + " " + r + " 0 0 " + (i < 2 ? 1 : 0) + " 0 -" + (2 * r), fill: rgba(theme.gold, 0.8) }));
      }),
      Array.from({ length: 18 }).map((_, i) => R("circle", { key: "s" + i, cx: (i * 173 + 50) % u.W, cy: (i * 229 + 80) % (u.H * 0.7), r: 1.5 + (i % 2), fill: rgba(theme.bone, 0.25 + 0.5 * Math.abs(Math.sin(t + i * 1.7))) })),
      [0, 1].map((i) => {
        const fl = Math.sin(t * (0.5 + i * 0.2) + i * 3) * 10;
        return R("g", { key: "c" + i, transform: "translate(" + (170 + i * (u.W - 360)) + "," + (u.H - 460 + i * 90) + ") rotate(" + (fl + (i ? 9 : -9)) + ")" },
          R("rect", { x: -56, y: -84, width: 112, height: 168, rx: 12, fill: rgba(theme.ink, 0.9), stroke: theme.gold, strokeWidth: 2.5 }),
          R("circle", { r: 30, fill: "none", stroke: rgba(theme.gold, 0.7), strokeWidth: 2 }),
          R("path", { d: "M -30 -54 h60 M -30 54 h60", stroke: rgba(theme.gold, 0.4), strokeWidth: 2 }));
      }),
      (function () {
        const fl = 1 + Math.sin(t * 7) * 0.18 + Math.sin(t * 13.7) * 0.1;
        return R("g", { transform: "translate(" + (u.W - 150) + "," + (u.H - 240) + ")" },
          R("rect", { x: -16, y: 0, width: 32, height: 90, rx: 8, fill: theme.bone, opacity: 0.85 }),
          R("ellipse", { cy: -18, rx: 9 * fl, ry: 20 * fl, fill: theme.gold }),
          R("ellipse", { cy: -14, rx: 4 * fl, ry: 10 * fl, fill: rgba("#fff8e0", 0.9) }));
      })()),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
