// DAYLIGHT CONSOLE — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        daylight-console-v2
//   renderer    film-daylight-console-v2
//   stage       landscape (1920x1080)
//   display     Bricolage Grotesque / body Instrument Sans
//   ground      #F7F4EE (paper), light
//   world       grid, beam, motes

const stage = require("../film_stage");

const SKIN = {
  id: "daylight-console-v2",
  label: "Daylight Console",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "landscape",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.45,"h":0.52},"montage":{"h":0.36},"statement":{"h":0.38}},


  // ---- type ----
  display: "Bricolage Grotesque", displayFallback: "system-ui, sans-serif",
  body: "Instrument Sans", bodyFallback: "system-ui, sans-serif",
  mono: "JetBrains Mono",
  em: 0.52,
  titleLine: 1.02, titleSpace: "-0.02em",
  titlePreset: "rise", itemPreset: "pop",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ paper: t.paper || "#F7F4EE", linen: t.linen || "#EFEAE1", ink: t.ink || "#14161A", slate: t.slate || "#4A4F57", indigo: t.indigo || "#2A4BD8", clay: t.clay || "#B0532A" }),
  accents: ["indigo","clay"],
  groundKey: "paper", inkKey: "ink", paperKey: "paper",
  dark: false,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "paper",
      "fg": "ink",
      "hi": "indigo",
      "world": true,
      "top": 180,
      "size": 88,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "pill",
        "c": "indigo"
      }
    },
    "statement": {
      "bg": "linen",
      "fg": "ink",
      "hi": "indigo",
      "world": false,
      "top": 160,
      "size": 88,
      "upper": false,
      "align": "left"
    },
    "feature": {
      "bg": "paper",
      "fg": "ink",
      "hi": "indigo",
      "world": true,
      "top": 140,
      "size": 72,
      "upper": false,
      "align": "left",
      "card": {
        "v": "glow",
        "bg": "linen",
        "r": 20,
        "glow": "indigo"
      },
      "chips": {
        "v": "pill",
        "colors": [
          "indigo",
          "clay",
          "slate"
        ],
        "text": "paper"
      }
    },
    "montage": {
      "bg": "linen",
      "fg": "ink",
      "hi": "indigo",
      "world": false,
      "top": 130,
      "size": 70,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "paper",
        "line": "indigo",
        "label": "ink",
        "r": 18,
        "labelSize": 24
      }
    },
    "stats": {
      "bg": "paper",
      "fg": "ink",
      "hi": "indigo",
      "world": true,
      "top": 150,
      "size": 72,
      "upper": false,
      "align": "left",
      "cols": [
        "indigo",
        "clay",
        "indigo"
      ],
      "num": 140
    },
    "cta": {
      "bg": "linen",
      "fg": "ink",
      "hi": "indigo",
      "world": false,
      "top": 140,
      "size": 68,
      "upper": false,
      "align": "left",
      "btn": {
        "v": "glow",
        "bg": "indigo",
        "c": "paper"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "paper",
      "fg": "ink",
      "hi": "indigo",
      "world": false,
      "top": 150,
      "size": 70,
      "upper": false,
      "align": "left",
      "cardBg": "linen",
      "line": "indigo"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["pushR","zoomIn","pushU","pushL","spin","zoomOut"], camMul: 3, camOff: 2,
  mag: {"x":0.65,"y":0.4,"rot":0.3,"skew":0,"zin":0.28,"zout":0.2,"driftX":7,"driftY":4,"driftZ":0.03,"slide":0.18,"inn":0.35,"out":0.85},
  ambient: 0.85,
  energy: 0.65,
  badge: "square",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"statement":"Swipe","feature":"Cursor","montage":"Scroll","stats":"Ring"},

  strings: {"brandName":"Daylight Console"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: theme.currentBg, stroke: "none" },
    R2("rect",{x:5,y:5,width:14,height:14,rx:3})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "linear-gradient(180deg, " + theme.paper + " 0%, " + bg + " 58%, " + theme.linen + " 100%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",null,Array.from({length:16}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/16,y1:0,x2:W*i/16,y2:H,stroke:rgba(theme.slate,0.08),strokeWidth:W*0});}).concat(Array.from({length:27}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/27,x2:W,y2:H*i/27,stroke:rgba(theme.slate,0.06),strokeWidth:W*0});}))),
    R("g",{transform:"rotate("+(-65+Math.sin(t*0.08)*6)+" "+(W*0.72)+" "+(H*0.42)+")"},
      R("rect",{x:W*0.72-W*0.25/2,y:-H*0.2,width:W*0.25,height:H*1.5,fill:rgba(theme.indigo,0.14)})),
    R("g",null,[{x:0.56,y:0.78,ph:2.84,r:4.02},{x:0.71,y:0.94,ph:3.15,r:3.01},{x:0.62,y:0.25,ph:3.65,r:4.27},{x:0.47,y:0.88,ph:4.33,r:5.11},{x:0.68,y:0.08,ph:0.49,r:2.03},{x:0.62,y:0.25,ph:3.06,r:4.73},{x:0.74,y:0.43,ph:3.13,r:5.73},{x:0.87,y:0.34,ph:0.2,r:3.73},{x:0.34,y:0.1,ph:4.5,r:4.67},{x:0.51,y:0.05,ph:4.27,r:5.12},{x:0.12,y:0.6,ph:0.53,r:3.42},{x:0.43,y:0.69,ph:2.55,r:3.25},{x:0.5,y:0.11,ph:4.88,r:4.01},{x:0.27,y:0.89,ph:5.24,r:4.93},{x:0.64,y:0.78,ph:4.17,r:4.11},{x:0.99,y:0.38,ph:4.09,r:2.3},{x:0.93,y:0.74,ph:1.54,r:4.79},{x:0.38,y:0.23,ph:4.65,r:4.13}].map(function(m,i){return R("circle",{key:i,cx:W*m.x+Math.sin(t*0.2+m.ph)*W*0.08,cy:(H*m.y+((t*10+i*90)%(H*0.6)))%H,r:m.r,fill:rgba(theme.clay,0.18)});}))
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
