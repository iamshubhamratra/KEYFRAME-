// PORCELAIN LAB — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        porcelain-lab
//   renderer    film-porcelain-lab
//   stage       portrait (1080x1920)
//   display     Space Grotesk / body Inter Tight
//   ground      #f6f8f9 (porcelain), light
//   world       grid, arcs, glow

const stage = require("../film_stage");

const SKIN = {
  id: "porcelain-lab",
  label: "Porcelain Lab",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "portrait",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.52,"h":0.28},"montage":{"h":0.14},"statement":{"h":0.16}},


  // ---- type ----
  display: "Space Grotesk", displayFallback: "Helvetica, Arial, sans-serif",
  body: "Inter Tight", bodyFallback: "system-ui, sans-serif",
  mono: "Space Mono",
  em: 0.52,
  titleLine: 1.05, titleSpace: "-0.02em",
  titlePreset: "rise", itemPreset: "streak",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ porcelain: t.porcelain || "#f6f8f9", studio: t.studio || "#e8ecee", paper: t.paper || "#ffffff", ink: t.ink || "#0f172a", slate: t.slate || "#64748b", cobalt: t.cobalt || "#2563eb", mist: t.mist || "#cbd5e1" }),
  accents: ["cobalt","slate","mist"],
  groundKey: "porcelain", inkKey: "ink", paperKey: "paper",
  dark: false,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "porcelain",
      "fg": "ink",
      "hi": "cobalt",
      "world": true,
      "top": 520,
      "size": 104,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "tag",
        "c": "slate"
      }
    },
    "statement": {
      "bg": "studio",
      "fg": "ink",
      "hi": "cobalt",
      "world": false,
      "top": 540,
      "size": 112,
      "upper": false,
      "align": "left",
      "card": {
        "v": "paper",
        "bg": "paper",
        "r": 12
      }
    },
    "feature": {
      "bg": "porcelain",
      "fg": "ink",
      "hi": "cobalt",
      "world": false,
      "top": 320,
      "size": 88,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "paper",
        "r": 16,
        "glow": "mist"
      },
      "chips": {
        "v": "outline",
        "colors": [
          "slate",
          "cobalt"
        ],
        "text": "ink"
      }
    },
    "montage": {
      "bg": "paper",
      "fg": "ink",
      "hi": "cobalt",
      "world": true,
      "top": 290,
      "size": 84,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "porcelain",
        "line": "mist",
        "label": "ink",
        "r": 12,
        "labelSize": 24
      }
    },
    "stats": {
      "bg": "porcelain",
      "fg": "ink",
      "hi": "cobalt",
      "world": true,
      "top": 460,
      "size": 92,
      "upper": false,
      "align": "left",
      "cols": [
        "cobalt",
        "slate",
        "ink"
      ],
      "num": 160
    },
    "cta": {
      "bg": "studio",
      "fg": "ink",
      "hi": "cobalt",
      "world": false,
      "top": 780,
      "size": 104,
      "upper": false,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "ink",
        "c": "paper"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "porcelain",
      "fg": "ink",
      "hi": "cobalt",
      "world": false,
      "top": 380,
      "size": 84,
      "upper": false,
      "align": "left",
      "cardBg": "paper",
      "line": "mist"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["pushL","zoomIn","pushU","spin","pushR","zoomOut"], camMul: 2, camOff: 1,
  mag: {"x":0.8,"y":0.4,"rot":0.3,"skew":0,"zin":0.28,"zout":0.22,"driftX":5,"driftY":4,"driftZ":0.02,"slide":0.35,"inn":0.3,"out":0.75},
  ambient: 0.8,
  energy: 1.4,
  badge: "outline",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"statement":"Notify","feature":"Cursor","montage":"Scroll","stats":"Ring"},

  strings: {"brandName":"Porcelain Lab"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M12 4 L20 12 L12 20 L4 12 Z"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "radial-gradient(120% 90% at 50% 12%, " + theme.studio + " 0%, " + bg + " 62%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",null,Array.from({length:24}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/24,y1:0,x2:W*i/24,y2:H,stroke:rgba(theme.slate,0.12),strokeWidth:W*0.02});}).concat(Array.from({length:41}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/41,x2:W,y2:H*i/41,stroke:rgba(theme.slate,0.08),strokeWidth:W*0.02});}))),
    R("g",null,Array.from({length:8}).map(function(_,i){
      var rr=W*0.35*(0.5+i*0.18), a0=t*0.2+i*1.1, a1=a0+1.7;
      var x0=W*0.5+Math.cos(a0)*rr, y0=H*0.4+Math.sin(a0)*rr;
      var x1=W*0.5+Math.cos(a1)*rr, y1=H*0.4+Math.sin(a1)*rr;
      return R("path",{key:i,d:"M"+x0+" "+y0+" A"+rr+" "+rr+" 0 0 1 "+x1+" "+y1,stroke:rgba(theme.cobalt,0.15),strokeWidth:W*0.02,fill:"none",strokeLinecap:"round"});
    })),
    R("circle",{cx:W*0.5,cy:H*0.3,r:W*0.51*(1+0.05*Math.sin(t*0.5+0.4)),fill:rgba(theme.paper,0.01)}),
    R("circle",{cx:W*0.5,cy:H*0.3,r:W*0.43*(1+0.05*Math.sin(t*0.5+0.8)),fill:rgba(theme.paper,0.03)}),
    R("circle",{cx:W*0.5,cy:H*0.3,r:W*0.34*(1+0.04*Math.sin(t*0.5+1.2)),fill:rgba(theme.paper,0.06)}),
    R("circle",{cx:W*0.5,cy:H*0.3,r:W*0.26*(1+0.04*Math.sin(t*0.5+1.6)),fill:rgba(theme.paper,0.09)}),
    R("circle",{cx:W*0.5,cy:H*0.3,r:W*0.17*(1+0.03*Math.sin(t*0.5+2)),fill:rgba(theme.paper,0.13)}),
    R("circle",{cx:W*0.5,cy:H*0.3,r:W*0.09*(1+0.02*Math.sin(t*0.5+2.4)),fill:rgba(theme.paper,0.26)})
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
