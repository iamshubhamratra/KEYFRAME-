// FRONT & ISOBAR — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by scripts/gen-film-skins.js from the handoff source
// (templete-design/keyframe-handoff/source/mega-pack-4.js, template "FrontIsobar").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Space Mono / body Familjen Grotesk. Camera set ["pushL","zoomOut","pushU","pushR","zoomIn","drop"].
// Entrances: title "rise", items "machete".
// Owns the Typing:terminal, Ring:gauge, Scroll:board, Notify:drop mechanics.
//
// palette / icon / World below are the AUTHORED functions, emitted verbatim — the world in
// particular is re-evaluated per seek against an SVG-DOM shim, so the backdrop is not a
// reinterpretation of the original, it IS the original.

const stage = require("../film_stage");

const SKIN = {
  id: "front-and-isobar",
  label: "Front & Isobar",

  // ---- type ----
  display: "Space Mono", displayFallback: "monospace",
  body: "Familjen Grotesk", bodyFallback: "sans-serif",
  mono: null,
  em: 0.58,
  titleLine: 1.14, titleSpace: "0.01em",
  titlePreset: "rise", itemPreset: "machete",

  // ---- palette ----
  // The authored function. Called with {} to get the design's own defaults; the brand's
  // accents are then hue-mapped onto `accents` slot by slot at their authored luminance,
  // and every other stop rotates onto the brand's lead hue — so a brand recolours the whole
  // world, not just the headline.
  palette: (t) => ({ chart: t.chart || "#f0ead8", isobar: t.isobar || "#2e5fb3", warn: "#e8641f", slate: "#37424e", ink: "#1e242c" }),
  accents: ["warn","isobar","chart"],
  groundKey: "chart", inkKey: "ink", paperKey: "chart",
  dark: false,

  // ---- per-beat look (verbatim) ----
  look: {
    "hook": {
      "bg": "chart",
      "fg": "ink",
      "hi": "warn",
      "world": true,
      "top": 320,
      "size": 96,
      "upper": true,
      "kicker": {
        "v": "tag",
        "bg": "isobar",
        "c": "chart"
      }
    },
    "statement": {
      "bg": "slate",
      "fg": "chart",
      "hi": "warn",
      "world": false,
      "top": 630,
      "size": 116,
      "upper": true
    },
    "feature": {
      "bg": "chart",
      "fg": "ink",
      "hi": "isobar",
      "world": true,
      "top": 250,
      "size": 82,
      "upper": true,
      "card": {
        "v": "frame",
        "bg": "slate",
        "r": 10,
        "line": "chart"
      },
      "chips": {
        "v": "square",
        "colors": [
          "isobar",
          "warn"
        ],
        "text": "chart"
      }
    },
    "montage": {
      "bg": "slate",
      "fg": "chart",
      "hi": "warn",
      "world": false,
      "top": 258,
      "size": 80,
      "upper": true,
      "tile": {
        "bg": "chart",
        "line": "ink",
        "label": "chart",
        "r": 8
      },
      "tilts": [
        0,
        0,
        0,
        0
      ]
    },
    "stats": {
      "bg": "chart",
      "fg": "ink",
      "hi": "isobar",
      "world": true,
      "cols": [
        "isobar",
        "warn",
        "slate"
      ],
      "num": 138,
      "upper": true,
      "rule": true
    },
    "cta": {
      "bg": "isobar",
      "fg": "chart",
      "hi": "warn",
      "world": false,
      "top": 500,
      "size": 96,
      "upper": true,
      "btn": {
        "v": "block",
        "bg": "chart",
        "c": "ink"
      },
      "logoShape": "rounded"
    },
    "app": {
      "bg": "chart",
      "fg": "ink",
      "hi": "isobar",
      "cardBg": "#faf6ea",
      "line": "ink",
      "world": true,
      "upper": true
    }
  },

  // ---- camera + motion ----
  cams: ["pushL","zoomOut","pushU","pushR","zoomIn","drop"], camMul: 5, camOff: 1,
  mag: {"rot":0.4,"slide":0.28,"driftX":6},
  ambient: 1.6,
  energy: 1,
  badge: "circle",
  

  // ---- the mechanics this pack owns ----
  // The handoff's promise is that no two templates share a mechanism. A pack that does not
  // declare a mechanic here never renders it.
  variants: {"Typing":"terminal","Ring":"gauge","Scroll":"board","Notify":"drop"},

  strings: {
    brandName: "Front & Isobar",
  },

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.5, strokeLinecap: "round" }, R2("path", { d: "M3 8h13a3 3 0 1 0-3-3M3 12h17a3 3 0 1 1-3 3M3 16h9a3 3 0 1 1-3 3" })),

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, progress, utils); no
  // Math.random and no Date, which is what lets it be re-evaluated deterministically on
  // every seek.
  World: (theme, t, p, u) => R("g", null,
      [0, 1, 2, 3].map((i) => {
        const pts = Array.from({ length: 8 }).map((_, k) => {
          const x = (k / 7) * u.W;
          const y = 260 + i * 120 + Math.sin(t * 0.4 + k * 0.8 + i * 1.2) * 30 + i * 10;
          return (k ? "L" : "M") + x + " " + y;
        }).join(" ");
        return R("path", { key: i, d: pts, fill: "none", stroke: rgba(theme.isobar, 0.45 - i * 0.07), strokeWidth: 3 });
      }),
      R("text", { x: 70, y: 250, fontFamily: '"Space Mono", monospace', fontSize: 24, fill: rgba(theme.isobar, 0.7) }, "1016"),
      R("text", { x: 70, y: 370, fontFamily: '"Space Mono", monospace', fontSize: 24, fill: rgba(theme.isobar, 0.6) }, "1012"),
      (function () {
        const fx = ((t * 60) % (u.W + 300)) - 150;
        return R("g", null,
          R("path", { d: "M " + fx + " " + (u.H - 500) + " q 120 -60 260 -20", fill: "none", stroke: theme.isobar, strokeWidth: 6 }),
          [0.25, 0.55, 0.85].map((f, k) => {
            const bx = fx + 260 * f, by = u.H - 500 - 40 * Math.sin(f * Math.PI);
            return R("polygon", { key: k, points: bx + "," + by + " " + (bx + 26) + "," + (by - 6) + " " + (bx + 8) + "," + (by - 26), fill: theme.isobar });
          }));
      })(),
      [[u.W - 260, 300], [u.W - 150, 420]].map(([x, y], i) => R("g", { key: "b" + i, transform: "translate(" + x + "," + y + ") rotate(" + (30 + Math.sin(t * 0.7 + i) * 14) + ")" },
        R("line", { x1: 0, y1: 0, x2: 0, y2: 70, stroke: theme.ink, strokeWidth: 4 }),
        R("line", { x1: 0, y1: 0, x2: 22, y2: -10, stroke: theme.ink, strokeWidth: 4 }),
        R("line", { x1: 0, y1: 14, x2: 18, y2: 5, stroke: theme.ink, strokeWidth: 4 }))),
      R("g", { opacity: 0.5 }, Array.from({ length: 6 }).map((_, i) => R("line", { key: i, x1: 90 + i * 26, y1: u.H - 320 + ((t * 140 + i * 20) % 60), x2: 80 + i * 26, y2: u.H - 296 + ((t * 140 + i * 20) % 60), stroke: theme.isobar, strokeWidth: 4, strokeLinecap: "round" }))),
      R("circle", { cx: u.W - 180, cy: u.H - 280, r: 44, fill: "none", stroke: theme.warn, strokeWidth: 5, strokeDasharray: "10 8", transform: "rotate(" + t * 30 + " " + (u.W - 180) + " " + (u.H - 280) + ")" }),
      R("text", { x: u.W - 180, y: u.H - 270, textAnchor: "middle", fontFamily: '"Space Mono", monospace', fontSize: 28, fill: theme.warn }, "L")),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
