// KERNEL BLACK — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        kernel-black
//   renderer    film-kernel-black
//   stage       portrait (1080x1920)
//   display     Big Shoulders Display / body Barlow Semi Condensed
//   ground      #000000 (void), dark
//   world       beam, grid, stripes

const stage = require("../film_stage");

const SKIN = {
  id: "kernel-black",
  label: "Kernel Black",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "portrait",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.42,"h":0.17},"montage":{"h":0.11},"statement":{"h":0.13}},


  // ---- type ----
  display: "Big Shoulders Display", displayFallback: "Impact, sans-serif",
  body: "Barlow Semi Condensed", bodyFallback: "system-ui, sans-serif",
  mono: "JetBrains Mono",
  em: 0.41,
  titleLine: 0.86, titleSpace: "-0.02em",
  titlePreset: "machete", itemPreset: "slam",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ void: t.void || "#000000", carbon: t.carbon || "#101014", graphite: t.graphite || "#1b1b21", ash: t.ash || "#8a9099", paper: t.paper || "#f2f4f7", ice: t.ice || "#7fe3ff", signal: t.signal || "#d92b12" }),
  accents: ["ice","signal","ash"],
  groundKey: "void", inkKey: "void", paperKey: "paper",
  dark: true,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "void",
      "fg": "paper",
      "hi": "ice",
      "world": true,
      "top": 760,
      "size": 176,
      "upper": true,
      "align": "left",
      "kicker": {
        "v": "tag",
        "c": "ice"
      }
    },
    "statement": {
      "bg": "void",
      "fg": "paper",
      "hi": "ice",
      "world": true,
      "top": 360,
      "size": 208,
      "upper": true,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "carbon",
        "r": 2,
        "glow": "ice"
      }
    },
    "feature": {
      "bg": "void",
      "fg": "paper",
      "hi": "ice",
      "world": false,
      "top": 200,
      "size": 132,
      "upper": true,
      "align": "left",
      "card": {
        "v": "glow",
        "bg": "carbon",
        "r": 2,
        "glow": "ice"
      },
      "chips": {
        "v": "square",
        "colors": [
          "graphite",
          "carbon"
        ],
        "text": "paper"
      }
    },
    "montage": {
      "bg": "paper",
      "fg": "void",
      "hi": "signal",
      "world": false,
      "top": 230,
      "size": 122,
      "upper": true,
      "align": "left",
      "tile": {
        "bg": "void",
        "line": "ash",
        "label": "paper",
        "r": 2,
        "labelSize": 26
      }
    },
    "stats": {
      "bg": "void",
      "fg": "paper",
      "hi": "ice",
      "world": false,
      "top": 330,
      "size": 118,
      "upper": true,
      "align": "left",
      "cols": [
        "ice",
        "paper",
        "signal"
      ],
      "num": 196
    },
    "cta": {
      "bg": "void",
      "fg": "paper",
      "hi": "ice",
      "world": true,
      "top": 780,
      "size": 168,
      "upper": true,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "ice",
        "c": "void"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "carbon",
      "fg": "paper",
      "hi": "ice",
      "world": false,
      "top": 380,
      "size": 120,
      "upper": false,
      "align": "left",
      "cardBg": "void",
      "line": "ash"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["zoomIn","pushU","drop","zoomOut","pushL","pushD"], camMul: 3, camOff: 1,
  mag: {"x":1.15,"y":0.9,"rot":0,"skew":0,"zin":0.6,"zout":0.5,"driftX":4,"driftY":10,"driftZ":0.02,"slide":0.34,"inn":0.14,"out":0.9},
  ambient: 0.8,
  energy: 1.35,
  badge: "square",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"feature":"Code","stats":"Ring"},

  strings: {"brandName":"Kernel Black"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M6 18 L6 11 M12 18 L12 5 M18 18 L18 8"})),

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",{transform:"rotate("+(90+Math.sin(t*0.28)*6)+" "+(W*0.52)+" "+(H*0.5)+")"},
      R("rect",{x:W*0.52-W*0.16/2,y:-H*0.2,width:W*0.16,height:H*1.5,fill:rgba(theme.ice,0.16)})),
    R("g",null,Array.from({length:14}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/14,y1:0,x2:W*i/14,y2:H,stroke:rgba(theme.ash,0.08),strokeWidth:W*0.02});}).concat(Array.from({length:24}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/24,x2:W,y2:H*i/24,stroke:rgba(theme.ash,0.06),strokeWidth:W*0.02});}))),
    R("g",{transform:"rotate(90 "+(W/2)+" "+(H/2)+")"},Array.from({length:6}).map(function(_,i){return R("rect",{key:i,x:-W*0.5+ (W*2*i/6) + Math.sin(t*0.66)*W*0.02,y:-H*0.5,width:W*0,height:H*2,fill:rgba(theme.paper,0.1)});}))
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
