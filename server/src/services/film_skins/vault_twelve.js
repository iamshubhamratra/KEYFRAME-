// VAULT TWELVE — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by scripts/gen-film-skins.js from the handoff source
// (templete-design/keyframe-handoff/source/mega-pack-6.js, template "VaultTwelve").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Chakra Petch / body IBM Plex Sans. Camera set ["zoomIn","pushR","spin","pushL","drop","zoomOut"].
// Entrances: title "machete", items "slam".
// Owns the Code:terminal, Ring:ring, Cursor:keys, Notify:drop mechanics.
//
// palette / icon / World below are the AUTHORED functions, emitted verbatim — the world in
// particular is re-evaluated per seek against an SVG-DOM shim, so the backdrop is not a
// reinterpretation of the original, it IS the original.

const stage = require("../film_stage");

const SKIN = {
  id: "vault-twelve",
  label: "Vault Twelve",

  // ---- type ----
  display: "Chakra Petch", displayFallback: "sans-serif",
  body: "IBM Plex Sans", bodyFallback: "sans-serif",
  mono: null,
  em: 0.52,
  titleLine: 1.04, titleSpace: "0.04em",
  titlePreset: "machete", itemPreset: "slam",

  // ---- palette ----
  // The authored function. Called with {} to get the design's own defaults; the brand's
  // accents are then hue-mapped onto `accents` slot by slot at their authored luminance,
  // and every other stop rotates onto the brand's lead hue — so a brand recolours the whole
  // world, not just the headline.
  palette: (t) => ({ steel: t.steel || "#141920", laser: t.laser || "#ff3355", bullion: "#e8c35a", chrome: "#c8d2dc", ink: "#0a0d11" }),
  accents: ["laser","bullion","steel"],
  groundKey: "steel", inkKey: "ink", paperKey: "chrome",
  dark: true,

  // ---- per-beat look (verbatim) ----
  look: {
    "hook": {
      "bg": "steel",
      "fg": "chrome",
      "hi": "laser",
      "world": true,
      "top": 320,
      "size": 112,
      "upper": true,
      "kicker": {
        "v": "tag",
        "bg": "laser",
        "c": "ink"
      }
    },
    "statement": {
      "bg": "ink",
      "fg": "chrome",
      "hi": "laser",
      "world": true,
      "top": 630,
      "size": 136,
      "upper": true
    },
    "feature": {
      "bg": "steel",
      "fg": "chrome",
      "hi": "bullion",
      "world": true,
      "top": 250,
      "size": 92,
      "upper": true,
      "card": {
        "v": "glow",
        "bg": "#0d1116",
        "glow": "laser",
        "r": 10,
        "line": "chrome"
      },
      "chips": {
        "v": "square",
        "colors": [
          "laser",
          "bullion",
          "chrome"
        ],
        "text": "ink"
      }
    },
    "montage": {
      "bg": "#0d1116",
      "fg": "chrome",
      "hi": "laser",
      "world": false,
      "top": 258,
      "size": 90,
      "upper": true,
      "tile": {
        "bg": "steel",
        "line": "chrome",
        "label": "bullion",
        "r": 8,
        "glow": "laser"
      },
      "tilts": [
        0,
        0,
        0,
        0
      ]
    },
    "stats": {
      "bg": "steel",
      "fg": "chrome",
      "hi": "laser",
      "world": true,
      "cols": [
        "laser",
        "bullion",
        "chrome"
      ],
      "num": 146,
      "upper": true,
      "glowNums": true,
      "rule": true
    },
    "cta": {
      "bg": "#0d1116",
      "fg": "chrome",
      "hi": "bullion",
      "world": true,
      "top": 500,
      "size": 110,
      "upper": true,
      "btn": {
        "v": "glow",
        "bg": "laser",
        "c": "ink"
      },
      "logoShape": "rounded"
    },
    "app": {
      "bg": "steel",
      "fg": "chrome",
      "hi": "laser",
      "cardBg": "#1c232c",
      "line": "chrome",
      "world": true,
      "upper": true
    }
  },

  // ---- camera + motion ----
  cams: ["zoomIn","pushR","spin","pushL","drop","zoomOut"], camMul: 7, camOff: 2,
  mag: {"rot":1,"skew":3,"inn":0.16},
  ambient: 1.9,
  energy: 1,
  badge: "circle",
  

  // ---- the mechanics this pack owns ----
  // The handoff's promise is that no two templates share a mechanism. A pack that does not
  // declare a mechanic here never renders it.
  variants: {"Code":"terminal","Ring":"ring","Cursor":"keys","Notify":"drop"},

  strings: {
    brandName: "Vault Twelve",
  },

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.5, strokeLinecap: "round" }, R2("rect", { x: 3, y: 4, width: 18, height: 16, rx: 3 }), R2("circle", { cx: 12, cy: 12, r: 4 }), R2("path", { d: "M12 8V6M12 18v-2M16 12h2M6 12h2" })),

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, progress, utils); no
  // Math.random and no Date, which is what lets it be re-evaluated deterministically on
  // every seek.
  World: (theme, t, p, u) => R("g", null,
      R("g", { transform: "translate(" + (u.W - 210) + "," + (u.H - 400) + ")" },
        R("circle", { r: 132, fill: rgba(theme.chrome, 0.08), stroke: rgba(theme.chrome, 0.4), strokeWidth: 8 }),
        R("g", { transform: "rotate(" + (Math.sin(t * 0.6) * 220) + ")" },
          Array.from({ length: 24 }).map((_, i) => R("line", { key: i, x1: Math.cos(i * Math.PI / 12) * 96, y1: Math.sin(i * Math.PI / 12) * 96, x2: Math.cos(i * Math.PI / 12) * 116, y2: Math.sin(i * Math.PI / 12) * 116, stroke: rgba(theme.chrome, i % 6 === 0 ? 0.85 : 0.35), strokeWidth: i % 6 === 0 ? 6 : 3 })),
          R("rect", { x: -9, y: -104, width: 18, height: 40, rx: 6, fill: theme.bullion })),
        R("circle", { r: 40, fill: rgba(theme.chrome, 0.22), stroke: rgba(theme.chrome, 0.5), strokeWidth: 5 }),
        R("circle", { r: 12, fill: theme.laser })),
      Array.from({ length: 5 }).map((_, i) => {
        const y = 260 + i * 130 + Math.sin(t * 0.9 + i * 1.3) * 26;
        return R("line", { key: "l" + i, x1: 60, y1: y, x2: u.W - 60, y2: y + Math.sin(t + i) * 30, stroke: rgba(theme.laser, 0.5), strokeWidth: 3, style: { filter: "drop-shadow(0 0 8px " + theme.laser + ")" } });
      }),
      [0, 1, 2, 3].map((i) => {
        const ph = (t * 0.4 + i * 0.25) % 1;
        const cy = 300 + ph * (u.H - 500);
        return R("ellipse", { key: "c" + i, cx: 180 + i * 90, cy, rx: 26, ry: 26 * Math.abs(Math.cos(ph * 9 + i)), fill: theme.bullion, opacity: 0.85 - ph * 0.3 });
      }),
      R("rect", { x: 70, y: u.H - 240, width: 220, height: 14, rx: 7, fill: rgba(theme.chrome, 0.2) }),
      R("rect", { x: 70, y: u.H - 240, width: 220 * (0.2 + Math.abs(Math.sin(t * 0.5)) * 0.8), height: 14, rx: 7, fill: theme.laser })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
