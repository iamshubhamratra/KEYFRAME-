// LUCID BENCH — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        lucid-bench
//   renderer    film-lucid-bench
//   stage       portrait (1080x1920)
//   display     Krona One / body Instrument Sans
//   ground      #f6f7f9 (chalk), light
//   world       grid, arcs, arcs

const stage = require("../film_stage");

const SKIN = {
  id: "lucid-bench",
  label: "Lucid Bench",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "portrait",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.52,"h":0.2},"montage":{"h":0.11},"statement":{"h":0.14}},


  // ---- type ----
  display: "Krona One", displayFallback: "Helvetica Neue, Arial, sans-serif",
  body: "Instrument Sans", bodyFallback: "system-ui, sans-serif",
  mono: "IBM Plex Mono",
  em: 0.7,
  titleLine: 1.18, titleSpace: "0.12em",
  titlePreset: "machete", itemPreset: "rise",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ chalk: t.chalk || "#f6f7f9", linen: t.linen || "#f2ece4", mist: t.mist || "#dfe5ec", steel: t.steel || "#5a6472", graphite: t.graphite || "#14171c", cobalt: t.cobalt || "#1f4fd8", amber: t.amber || "#9a4a06" }),
  accents: ["cobalt","amber","steel"],
  groundKey: "chalk", inkKey: "graphite", paperKey: "chalk",
  dark: false,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "chalk",
      "fg": "graphite",
      "hi": "cobalt",
      "world": true,
      "top": 1180,
      "size": 74,
      "upper": true,
      "align": "left",
      "kicker": {
        "v": "tag",
        "c": "cobalt"
      }
    },
    "statement": {
      "bg": "linen",
      "fg": "graphite",
      "hi": "cobalt",
      "world": false,
      "top": 1090,
      "size": 96,
      "upper": true,
      "align": "left"
    },
    "feature": {
      "bg": "chalk",
      "fg": "graphite",
      "hi": "cobalt",
      "world": true,
      "top": 1010,
      "size": 54,
      "upper": true,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "chalk",
        "r": 4
      },
      "chips": {
        "v": "outline",
        "colors": [
          "steel",
          "cobalt"
        ],
        "text": "graphite"
      }
    },
    "montage": {
      "bg": "mist",
      "fg": "graphite",
      "hi": "cobalt",
      "world": true,
      "top": 980,
      "size": 50,
      "upper": true,
      "align": "left",
      "tile": {
        "bg": "chalk",
        "line": "steel",
        "label": "graphite",
        "r": 4,
        "labelSize": 24
      }
    },
    "stats": {
      "bg": "chalk",
      "fg": "graphite",
      "hi": "cobalt",
      "world": true,
      "top": 1020,
      "size": 52,
      "upper": true,
      "align": "left",
      "cols": [
        "cobalt",
        "graphite",
        "amber"
      ],
      "num": 122
    },
    "cta": {
      "bg": "mist",
      "fg": "graphite",
      "hi": "cobalt",
      "world": true,
      "top": 1215,
      "size": 70,
      "upper": true,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "graphite",
        "c": "chalk"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "chalk",
      "fg": "graphite",
      "hi": "cobalt",
      "world": false,
      "top": 960,
      "size": 48,
      "upper": true,
      "align": "left",
      "cardBg": "chalk",
      "line": "steel"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["pushR","pushL","hopU","spin","pushU","zoomOut"], camMul: 4, camOff: 1,
  mag: {"x":1.15,"y":0.5,"rot":0.9,"skew":0.1,"zin":0.3,"zout":0.4,"driftX":16,"driftY":10,"driftZ":0.06,"slide":0.34,"inn":0.18,"out":0.9},
  ambient: 0.8,
  energy: 1.35,
  badge: "outline",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"feature":"Cursor","stats":"Ring"},

  strings: {"brandName":"Lucid Bench"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M12 5 L12 19 M5 12 L19 12"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "linear-gradient(155deg, " + theme.linen + " 0%, " + bg + " 58%, " + theme.mist + " 100%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",null,Array.from({length:16}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/16,y1:0,x2:W*i/16,y2:H,stroke:rgba(theme.steel,0.14),strokeWidth:W*0});}).concat(Array.from({length:27}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/27,x2:W,y2:H*i/27,stroke:rgba(theme.steel,0.1),strokeWidth:W*0});}))),
    R("g",null,Array.from({length:3}).map(function(_,i){
      var rr=W*0.46*(0.5+i*0.18), a0=t*0.7+i*1.1, a1=a0+1.7;
      var x0=W*0.7+Math.cos(a0)*rr, y0=H*0.38+Math.sin(a0)*rr;
      var x1=W*0.7+Math.cos(a1)*rr, y1=H*0.38+Math.sin(a1)*rr;
      return R("path",{key:i,d:"M"+x0+" "+y0+" A"+rr+" "+rr+" 0 0 1 "+x1+" "+y1,stroke:rgba(theme.cobalt,0.22),strokeWidth:W*0,fill:"none",strokeLinecap:"round"});
    })),
    R("g",null,Array.from({length:2}).map(function(_,i){
      var rr=W*0.3*(0.5+i*0.18), a0=t*0.45+i*1.1, a1=a0+1.7;
      var x0=W*0.26+Math.cos(a0)*rr, y0=H*0.66+Math.sin(a0)*rr;
      var x1=W*0.26+Math.cos(a1)*rr, y1=H*0.66+Math.sin(a1)*rr;
      return R("path",{key:i,d:"M"+x0+" "+y0+" A"+rr+" "+rr+" 0 0 1 "+x1+" "+y1,stroke:rgba(theme.amber,0.16),strokeWidth:W*0,fill:"none",strokeLinecap:"round"});
    }))
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
