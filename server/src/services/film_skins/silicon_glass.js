// SILICON GLASS — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        silicon-glass
//   renderer    film-silicon-glass
//   stage       portrait (1080x1920)
//   display     Unbounded / body Space Grotesk
//   ground      #090d16 (darkBg), dark
//   world       grid, orb, orb

const stage = require("../film_stage");

const SKIN = {
  id: "silicon-glass",
  label: "Silicon Glass",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "portrait",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.92,"h":0.42},"montage":{"h":0.22},"statement":{"h":0.32}},


  // ---- type ----
  display: "Unbounded", displayFallback: "system-ui, sans-serif",
  body: "Space Grotesk", bodyFallback: "system-ui, sans-serif",
  mono: "JetBrains Mono",
  em: 0.52,
  titleLine: 1.05, titleSpace: "-0.02em",
  titlePreset: "rise", itemPreset: "pop",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ darkBg: t.darkBg || "#090d16", panel: t.panel || "#131b2e", cyanGlow: t.cyanGlow || "#00f0ff", emeraldGlow: t.emeraldGlow || "#00ff9d", paper: t.paper || "#f0f6fc", mutedText: t.mutedText || "#8b9bb4" }),
  accents: ["cyanGlow","emeraldGlow","panel"],
  groundKey: "darkBg", inkKey: "darkBg", paperKey: "paper",
  dark: true,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "darkBg",
      "fg": "paper",
      "hi": "cyanGlow",
      "world": true,
      "top": 880,
      "size": 128,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "outline",
        "c": "cyanGlow"
      }
    },
    "statement": {
      "bg": "panel",
      "fg": "paper",
      "hi": "emeraldGlow",
      "world": false,
      "top": 580,
      "size": 144,
      "upper": false,
      "align": "left"
    },
    "feature": {
      "bg": "darkBg",
      "fg": "paper",
      "hi": "cyanGlow",
      "world": false,
      "top": 240,
      "size": 100,
      "upper": false,
      "align": "left",
      "card": {
        "v": "glow",
        "bg": "panel",
        "r": 16,
        "glow": "cyanGlow"
      },
      "chips": {
        "v": "pill",
        "colors": [
          "panel",
          "panel"
        ],
        "text": "paper"
      }
    },
    "montage": {
      "bg": "panel",
      "fg": "paper",
      "hi": "cyanGlow",
      "world": false,
      "top": 260,
      "size": 96,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "darkBg",
        "line": "cyanGlow",
        "label": "paper",
        "r": 16,
        "labelSize": 28
      }
    },
    "stats": {
      "bg": "darkBg",
      "fg": "paper",
      "hi": "emeraldGlow",
      "world": true,
      "top": 380,
      "size": 100,
      "upper": false,
      "align": "left",
      "cols": [
        "cyanGlow",
        "emeraldGlow",
        "paper"
      ],
      "num": 160
    },
    "cta": {
      "bg": "darkBg",
      "fg": "paper",
      "hi": "cyanGlow",
      "world": true,
      "top": 880,
      "size": 118,
      "upper": false,
      "align": "center",
      "btn": {
        "v": "glow",
        "bg": "cyanGlow",
        "c": "darkBg"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "panel",
      "fg": "paper",
      "hi": "cyanGlow",
      "world": false,
      "top": 380,
      "size": 96,
      "upper": false,
      "align": "left",
      "cardBg": "darkBg",
      "line": "cyanGlow"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["pushU","zoomIn","drop","pushD","zoomOut","hopU"], camMul: 2, camOff: 1,
  mag: {"x":1,"y":0.5,"rot":0.4,"skew":0,"zin":0.35,"zout":0.3,"driftX":6,"driftY":8,"driftZ":0.03,"slide":0.2,"inn":0.2,"out":0.8},
  ambient: 1.2,
  energy: 1,
  badge: "square",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"statement":"Notify","feature":"Code","stats":"Ring"},

  strings: {"brandName":"Silicon Glass"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M6 18 L6 11 M12 18 L12 5 M18 18 L18 8"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "radial-gradient(120% 90% at 50% 12%, " + theme.darkBg + " 0%, " + bg + " 62%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",null,Array.from({length:8}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/8,y1:0,x2:W*i/8,y2:H,stroke:rgba(theme.cyanGlow,0.08),strokeWidth:W*0});}).concat(Array.from({length:14}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/14,x2:W,y2:H*i/14,stroke:rgba(theme.cyanGlow,0.06),strokeWidth:W*0});}))),
    R("circle",{cx:W*0.8+Math.sin(t*0.24)*W*0.1,cy:H*0.2+Math.cos(t*0.19)*H*0.05,r:W*0.45,fill:rgba(theme.cyanGlow,0.02)}),
    R("circle",{cx:W*0.8+Math.sin(t*0.24)*W*0.1,cy:H*0.2+Math.cos(t*0.19)*H*0.05,r:W*0.34,fill:rgba(theme.cyanGlow,0.05)}),
    R("circle",{cx:W*0.8+Math.sin(t*0.24)*W*0.1,cy:H*0.2+Math.cos(t*0.19)*H*0.05,r:W*0.23,fill:rgba(theme.cyanGlow,0.07)}),
    R("circle",{cx:W*0.8+Math.sin(t*0.24)*W*0.1,cy:H*0.2+Math.cos(t*0.19)*H*0.05,r:W*0.13,fill:rgba(theme.cyanGlow,0.08)}),
    R("circle",{cx:W*0.2+Math.sin(t*0.18)*W*0.1,cy:H*0.75+Math.cos(t*0.14)*H*0.05,r:W*0.5,fill:rgba(theme.emeraldGlow,0.02)}),
    R("circle",{cx:W*0.2+Math.sin(t*0.18)*W*0.1,cy:H*0.75+Math.cos(t*0.14)*H*0.05,r:W*0.38,fill:rgba(theme.emeraldGlow,0.04)}),
    R("circle",{cx:W*0.2+Math.sin(t*0.18)*W*0.1,cy:H*0.75+Math.cos(t*0.14)*H*0.05,r:W*0.26,fill:rgba(theme.emeraldGlow,0.06)}),
    R("circle",{cx:W*0.2+Math.sin(t*0.18)*W*0.1,cy:H*0.75+Math.cos(t*0.14)*H*0.05,r:W*0.14,fill:rgba(theme.emeraldGlow,0.07)})
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
