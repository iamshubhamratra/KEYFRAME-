// ROCKET NIGHTS — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by scripts/gen-film-skins.js from the handoff source
// (templete-design/keyframe-handoff/source/mega-pack-4.js, template "RocketNights").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Paytone One / body Outfit. Camera set ["pushU","zoomIn","drop","spin","pushL","pushR"].
// Entrances: title "slam", items "pop".
// Owns the Code:terminal, Notify:pop, Morph:flap, Swipe:flip mechanics.
//
// palette / icon / World below are the AUTHORED functions, emitted verbatim — the world in
// particular is re-evaluated per seek against an SVG-DOM shim, so the backdrop is not a
// reinterpretation of the original, it IS the original.

const stage = require("../film_stage");

const SKIN = {
  id: "rocket-nights",
  label: "Rocket Nights",

  // ---- type ----
  display: "Paytone One", displayFallback: "sans-serif",
  body: "Outfit", bodyFallback: "sans-serif",
  mono: null,
  em: 0.6,
  titleLine: 1.04, titleSpace: "0",
  titlePreset: "slam", itemPreset: "pop",

  // ---- palette ----
  // The authored function. Called with {} to get the design's own defaults; the brand's
  // accents are then hue-mapped onto `accents` slot by slot at their authored luminance,
  // and every other stop rotates onto the brand's lead hue — so a brand recolours the whole
  // world, not just the headline.
  palette: (t) => ({ night: t.night || "#10101c", rocket: t.rocket || "#ff4757", spark: "#ffd23c", violet: "#7c5cff", smoke: "#e8e6f0" }),
  accents: ["spark","night","rocket"],
  groundKey: "night", inkKey: "night", paperKey: "smoke",
  dark: true,

  // ---- per-beat look (verbatim) ----
  look: {
    "hook": {
      "bg": "night",
      "fg": "smoke",
      "hi": "spark",
      "world": true,
      "top": 310,
      "size": 124,
      "upper": true,
      "kicker": {
        "v": "pill",
        "bg": "rocket",
        "c": "smoke"
      }
    },
    "statement": {
      "bg": "rocket",
      "fg": "smoke",
      "hi": "night",
      "world": false,
      "top": 630,
      "size": 148,
      "upper": true
    },
    "feature": {
      "bg": "night",
      "fg": "smoke",
      "hi": "spark",
      "world": true,
      "top": 248,
      "size": 96,
      "upper": true,
      "card": {
        "v": "glow",
        "bg": "#0c0c16",
        "glow": "spark",
        "r": 18,
        "line": "smoke"
      },
      "chips": {
        "v": "pill",
        "colors": [
          "rocket",
          "violet",
          "spark"
        ],
        "text": "night"
      }
    },
    "montage": {
      "bg": "#0c0c16",
      "fg": "smoke",
      "hi": "spark",
      "world": true,
      "top": 258,
      "size": 92,
      "upper": true,
      "tile": {
        "bg": "night",
        "line": "smoke",
        "label": "spark",
        "r": 14,
        "glow": "violet"
      },
      "tilts": [
        -2.5,
        2,
        1.5,
        -2
      ]
    },
    "stats": {
      "bg": "night",
      "fg": "smoke",
      "hi": "spark",
      "world": true,
      "cols": [
        "spark",
        "rocket",
        "violet"
      ],
      "num": 150,
      "upper": true,
      "glowNums": true
    },
    "cta": {
      "bg": "#0c0c16",
      "fg": "smoke",
      "hi": "spark",
      "world": true,
      "top": 500,
      "size": 118,
      "upper": true,
      "btn": {
        "v": "glow",
        "bg": "rocket",
        "c": "smoke"
      },
      "logoShape": "circle"
    },
    "app": {
      "bg": "night",
      "fg": "smoke",
      "hi": "spark",
      "cardBg": "#1a1a2a",
      "line": "smoke",
      "world": true,
      "upper": true
    }
  },

  // ---- camera + motion ----
  cams: ["pushU","zoomIn","drop","spin","pushL","pushR"], camMul: 7, camOff: 1,
  mag: {"rot":1.1,"driftY":10,"inn":0.18},
  ambient: 1.9,
  energy: 1,
  badge: "circle",
  

  // ---- the mechanics this pack owns ----
  // The handoff's promise is that no two templates share a mechanism. A pack that does not
  // declare a mechanic here never renders it.
  variants: {"Code":"terminal","Notify":"pop","Morph":"flap","Swipe":"flip"},

  strings: {
    brandName: "Rocket Nights",
  },

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.5, strokeLinecap: "round", strokeLinejoin: "round" }, R2("path", { d: "M12 2c3 3 4 7 4 10l-4 4-4-4c0-3 1-7 4-10zM8 14l-3 3M16 14l3 3M12 16v5" })),

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, progress, utils); no
  // Math.random and no Date, which is what lets it be re-evaluated deterministically on
  // every seek.
  World: (theme, t, p, u) => R("g", null,
      [0, 1, 2].map((i) => {
        const ph = (t * 0.34 + i * 0.33) % 1;
        const cx = 200 + i * 330, cy = 700 - i * 120;
        const cols = [theme.rocket, theme.spark, theme.violet];
        if (ph < 0.35) {
          const q = ph / 0.35;
          return R("g", { key: i },
            R("line", { x1: cx, y1: u.H - 200 - q * (u.H - 200 - cy), x2: cx, y2: u.H - 140 - q * (u.H - 200 - cy), stroke: cols[i], strokeWidth: 5, strokeLinecap: "round" }),
            R("circle", { cx, cy: u.H - 200 - q * (u.H - 200 - cy), r: 6, fill: theme.smoke }));
        }
        const q = (ph - 0.35) / 0.65;
        return R("g", { key: i, transform: "translate(" + cx + "," + cy + ")", opacity: 1 - q },
          Array.from({ length: 12 }).map((_, k) => {
            const a = k * Math.PI / 6;
            return R("line", { key: k, x1: Math.cos(a) * q * 40, y1: Math.sin(a) * q * 40, x2: Math.cos(a) * (30 + q * 170), y2: Math.sin(a) * (30 + q * 170), stroke: cols[i], strokeWidth: 4, strokeLinecap: "round" });
          }),
          Array.from({ length: 8 }).map((_, k) => R("circle", { key: "d" + k, cx: Math.cos(k * Math.PI / 4 + 0.4) * (40 + q * 200), cy: Math.sin(k * Math.PI / 4 + 0.4) * (40 + q * 200) + q * q * 60, r: 4, fill: theme.smoke, opacity: 0.8 })));
      }),
      Array.from({ length: 14 }).map((_, i) => R("circle", { key: "s" + i, cx: (i * 149 + 40) % u.W, cy: (i * 211 + 60) % (u.H * 0.5), r: 1.5, fill: rgba(theme.smoke, 0.3 + 0.5 * Math.abs(Math.sin(t * 2 + i * 1.9))) })),
      R("path", { d: "M -20 " + u.H + " L -20 " + (u.H - 170) + " Q " + (u.W * 0.3) + " " + (u.H - 260) + " " + (u.W * 0.6) + " " + (u.H - 180) + " T " + (u.W + 20) + " " + (u.H - 210) + " L " + (u.W + 20) + " " + u.H + " Z", fill: "#07070e" })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
