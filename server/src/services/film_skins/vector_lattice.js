// VECTOR LATTICE — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        vector-lattice
//   renderer    film-vector-lattice
//   stage       portrait (1080x1920)
//   display     Unbounded / body Space Grotesk
//   ground      #05070f (obsidian), dark
//   world       grid, beam, glow

const stage = require("../film_stage");

const SKIN = {
  id: "vector-lattice",
  label: "Vector Lattice",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "portrait",

  // This template's own picture boxes, as fractions of the stage. film_beats.boxOf reads them when
  // it draws, and scripts/gen-film-packs derives the pack.json media contract from the same
  // numbers — so the manifest can never promise a box the film does not draw.
  boxes: {"feature":{"w":0.72,"h":0.28},"montage":{"h":0.16},"statement":{"h":0.22}},


  // ---- type ----
  display: "Unbounded", displayFallback: "sans-serif",
  body: "Space Grotesk", bodyFallback: "system-ui, sans-serif",
  mono: "JetBrains Mono",
  em: 0.54,
  titleLine: 0.98, titleSpace: "-0.02em",
  titlePreset: "slam", itemPreset: "pop",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ obsidian: t.obsidian || "#05070f", violet: t.violet || "#140a28", slate: t.slate || "#162032", cyan: t.cyan || "#00f0ff", lime: t.lime || "#70ff00", paper: t.paper || "#f0f6ff", ink: t.ink || "#03050a" }),
  accents: ["cyan","lime","violet"],
  groundKey: "obsidian", inkKey: "ink", paperKey: "paper",
  dark: true,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "obsidian",
      "fg": "paper",
      "hi": "cyan",
      "world": true,
      "top": 880,
      "size": 156,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "outline",
        "c": "cyan"
      }
    },
    "statement": {
      "bg": "violet",
      "fg": "paper",
      "hi": "lime",
      "world": false,
      "top": 580,
      "size": 172,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "slate",
        "r": 16
      }
    },
    "feature": {
      "bg": "obsidian",
      "fg": "paper",
      "hi": "cyan",
      "world": false,
      "top": 240,
      "size": 112,
      "upper": false,
      "align": "left",
      "card": {
        "v": "glow",
        "bg": "slate",
        "r": 20,
        "glow": "cyan"
      },
      "chips": {
        "v": "pill",
        "colors": [
          "cyan",
          "lime"
        ],
        "text": "ink"
      }
    },
    "montage": {
      "bg": "violet",
      "fg": "paper",
      "hi": "lime",
      "world": false,
      "top": 260,
      "size": 108,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "slate",
        "line": "cyan",
        "label": "paper",
        "r": 16,
        "labelSize": 28
      }
    },
    "stats": {
      "bg": "obsidian",
      "fg": "paper",
      "hi": "cyan",
      "world": true,
      "top": 380,
      "size": 112,
      "upper": false,
      "align": "left",
      "cols": [
        "cyan",
        "lime",
        "paper"
      ],
      "num": 180
    },
    "cta": {
      "bg": "obsidian",
      "fg": "paper",
      "hi": "cyan",
      "world": true,
      "top": 890,
      "size": 140,
      "upper": false,
      "align": "left",
      "btn": {
        "v": "glow",
        "bg": "cyan",
        "c": "ink"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "violet",
      "fg": "paper",
      "hi": "cyan",
      "world": false,
      "top": 380,
      "size": 108,
      "upper": false,
      "align": "left",
      "cardBg": "slate",
      "line": "cyan"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["spin","pushU","zoomIn","drop","pushL","pushD"], camMul: 4, camOff: 2,
  mag: {"x":1.2,"y":0.7,"rot":1.1,"skew":0,"zin":0.5,"zout":0.4,"driftX":12,"driftY":10,"driftZ":0.06,"slide":0.3,"inn":0.3,"out":0.85},
  ambient: 1.6,
  energy: 1.4,
  badge: "outline",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"statement":"Morph","feature":"Code","stats":"Ring"},

  strings: {"brandName":"Vector Lattice"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M13 3 L6 13 L11 13 L10 21 L18 10 L13 10 Z"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "radial-gradient(120% 90% at 50% 12%, " + theme.obsidian + " 0%, " + bg + " 62%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",null,Array.from({length:18}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/18,y1:0,x2:W*i/18,y2:H,stroke:rgba(theme.cyan,0.22),strokeWidth:W*0.02});}).concat(Array.from({length:31}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/31,x2:W,y2:H*i/31,stroke:rgba(theme.cyan,0.15),strokeWidth:W*0.02});}))),
    R("g",{transform:"rotate("+(45+Math.sin(t*0.4)*6)+" "+(W*0.5)+" "+(H*0.2)+")"},
      R("rect",{x:W*0.5-W*0.18/2,y:-H*0.2,width:W*0.18,height:H*1.5,fill:rgba(theme.lime,0.25)})),
    R("circle",{cx:W*0.5,cy:H*0.5,r:W*0.56*(1+0.05*Math.sin(t*1.2+0.4)),fill:rgba(theme.cyan,0.01)}),
    R("circle",{cx:W*0.5,cy:H*0.5,r:W*0.46*(1+0.05*Math.sin(t*1.2+0.8)),fill:rgba(theme.cyan,0.03)}),
    R("circle",{cx:W*0.5,cy:H*0.5,r:W*0.37*(1+0.04*Math.sin(t*1.2+1.2)),fill:rgba(theme.cyan,0.05)}),
    R("circle",{cx:W*0.5,cy:H*0.5,r:W*0.28*(1+0.04*Math.sin(t*1.2+1.6)),fill:rgba(theme.cyan,0.08)}),
    R("circle",{cx:W*0.5,cy:H*0.5,r:W*0.19*(1+0.03*Math.sin(t*1.2+2)),fill:rgba(theme.cyan,0.11)}),
    R("circle",{cx:W*0.5,cy:H*0.5,r:W*0.09*(1+0.02*Math.sin(t*1.2+2.4)),fill:rgba(theme.cyan,0.23)})
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
