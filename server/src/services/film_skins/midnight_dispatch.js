// MIDNIGHT DISPATCH — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by the KEYFRAME admin template generator (src/templates/emit.js) from a validated
// TemplateSpec. Do not hand-edit: regenerating the template overwrites this file. To change the
// design, edit the spec on the template record and regenerate.
//
// Every executable line below was written by emit.js, not by the design model — the model
// supplied only the data (palette, per-beat look, camera set, world layer list). See the
// comment at the top of src/templates/emit.js for why that boundary exists.
//
//   slug        midnight-dispatch
//   renderer    film-midnight-dispatch
//   stage       landscape (1920x1080)
//   display     Big Shoulders Display / body Barlow Semi Condensed
//   ground      #05070a (ink), dark
//   world       stripes, glow, beam, grid

const stage = require("../film_stage");

const SKIN = {
  id: "midnight-dispatch",
  label: "Midnight Dispatch",

  // The AUTHORED stage. film_stage.build() adopts it before emitting any geometry, so every
  // number below is in pixels of this frame and film_beats picks the matching layout family.
  stage: "landscape",


  // ---- type ----
  display: "Big Shoulders Display", displayFallback: "Impact, sans-serif",
  body: "Barlow Semi Condensed", bodyFallback: "system-ui, sans-serif",
  mono: "IBM Plex Mono",
  em: 0.41,
  titleLine: 0.94, titleSpace: "-0.02em",
  titlePreset: "slam", itemPreset: "stamp",

  // ---- palette ----
  // Called with {} for the authored defaults. film_stage.buildTheme then hue-maps the brand's
  // accents onto `accents` slot by slot at their authored luminance and rotates every other
  // stop onto the brand's lead hue — so a customer's brand recolours the whole design, not just
  // the headline. That is why every colour below is a ROLE the theme resolves, and why the
  // world and ground read `theme.<role>` rather than baking hexes.
  palette: (t) => ({ ink: t.ink || "#05070a", carbon: t.carbon || "#0f141b", midnight: t.midnight || "#0a2d52", signal: t.signal || "#0d77e7", amber: t.amber || "#ffb020", paper: t.paper || "#f5f2ea", ash: t.ash || "#8d97a5" }),
  accents: ["signal","amber","ash"],
  groundKey: "ink", inkKey: "ink", paperKey: "paper",
  dark: true,

  // ---- per-beat look ----
  // `world` is load-bearing, not decoration: each beat's copy colour is authored against its
  // OWN field, so a beat that says false is drawn on its flat bg and a beat that says true is
  // drawn over the animated backdrop.
  look: {
    "hook": {
      "bg": "ink",
      "fg": "paper",
      "hi": "amber",
      "world": true,
      "top": 300,
      "size": 138,
      "upper": false,
      "align": "left",
      "kicker": {
        "v": "outline",
        "c": "amber"
      }
    },
    "statement": {
      "bg": "midnight",
      "fg": "paper",
      "hi": "amber",
      "world": false,
      "top": 250,
      "size": 150,
      "upper": false,
      "align": "left"
    },
    "feature": {
      "bg": "paper",
      "fg": "ink",
      "hi": "signal",
      "world": false,
      "top": 170,
      "size": 108,
      "upper": false,
      "align": "left",
      "card": {
        "v": "frame",
        "bg": "ink",
        "r": 6,
        "glow": "signal"
      },
      "chips": {
        "v": "outline",
        "colors": [
          "signal",
          "amber"
        ],
        "text": "ink"
      }
    },
    "montage": {
      "bg": "ink",
      "fg": "paper",
      "hi": "signal",
      "world": false,
      "top": 180,
      "size": 100,
      "upper": false,
      "align": "left",
      "tile": {
        "bg": "carbon",
        "line": "signal",
        "label": "paper",
        "r": 6,
        "labelSize": 28
      }
    },
    "stats": {
      "bg": "carbon",
      "fg": "paper",
      "hi": "amber",
      "world": true,
      "top": 240,
      "size": 96,
      "upper": false,
      "align": "left",
      "cols": [
        "amber",
        "signal",
        "paper"
      ],
      "num": 172
    },
    "cta": {
      "bg": "ink",
      "fg": "paper",
      "hi": "amber",
      "world": true,
      "top": 320,
      "size": 124,
      "upper": false,
      "align": "left",
      "btn": {
        "v": "block",
        "bg": "amber",
        "c": "ink"
      },
      "logoShape": "square"
    },
    "app": {
      "bg": "carbon",
      "fg": "paper",
      "hi": "signal",
      "world": false,
      "top": 230,
      "size": 98,
      "upper": false,
      "align": "left",
      "cardBg": "ink",
      "line": "ash"
    }
  },

  // ---- camera + motion ----
  // Indexed (sceneIndex * camMul + camOff) % 6 by film_stage, so adjacent scenes take different
  // camera moves rather than the whole film sharing one.
  cams: ["pushL","zoomIn","pushU","pushR","drop","zoomOut"], camMul: 2, camOff: 1,
  mag: {"x":1.1,"y":0.4,"rot":0.25,"skew":0,"zin":0.3,"zout":0.26,"driftX":10,"driftY":4,"driftZ":0.03,"slide":0.3,"inn":0.16,"out":0.7},
  ambient: 1.1,
  energy: 1.3,
  badge: "square",

  // ---- the mechanics this pack owns ----
  // A beat that ends up holding no picture is expressed as one of these instead of shipping a
  // bare text slide. A pack that declares none renders only the six authored acts.
  variants: {"statement":"Notify","feature":"Typing","stats":"Ring"},

  strings: {"brandName":"Midnight Dispatch"},

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 24, height: 24, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round" },
    R2("path",{d:"M6 18 L6 11 M12 18 L12 5 M18 18 L18 8"})),

  // The ground treatment, resolved against the live (possibly re-hued) theme.
  groundCss: (theme, bg) => "linear-gradient(155deg, " + theme.midnight + " 0%, " + bg + " 58%, " + theme.ink + " 100%)",

  // ---- the world ----
  // The animated backdrop. Pure in (theme, t, progress, utils) — no Math.random and no Date,
  // which is what lets film_stage re-evaluate it deterministically on every seek. All geometry
  // was resolved at generation time from the spec's layer list, so the numbers below are
  // literals and the render page runs no RNG at all.
  World: (theme, t, p, u) => {
    var W = u.W, H = u.H;
    return R("g", null,
    R("g",{transform:"rotate(0 "+(W/2)+" "+(H/2)+")"},Array.from({length:20}).map(function(_,i){return R("rect",{key:i,x:-W*0.5+ (W*2*i/20) + Math.sin(t*0.08)*W*0.02,y:-H*0.5,width:W*0,height:H*2,fill:rgba(theme.paper,0.06)});})),
    R("circle",{cx:W*0.74,cy:H*0.3,r:W*0.51*(1+0.05*Math.sin(t*0.5+0.4)),fill:rgba(theme.signal,0.01)}),
    R("circle",{cx:W*0.74,cy:H*0.3,r:W*0.43*(1+0.05*Math.sin(t*0.5+0.8)),fill:rgba(theme.signal,0.02)}),
    R("circle",{cx:W*0.74,cy:H*0.3,r:W*0.34*(1+0.04*Math.sin(t*0.5+1.2)),fill:rgba(theme.signal,0.03)}),
    R("circle",{cx:W*0.74,cy:H*0.3,r:W*0.26*(1+0.04*Math.sin(t*0.5+1.6)),fill:rgba(theme.signal,0.05)}),
    R("circle",{cx:W*0.74,cy:H*0.3,r:W*0.17*(1+0.03*Math.sin(t*0.5+2)),fill:rgba(theme.signal,0.08)}),
    R("circle",{cx:W*0.74,cy:H*0.3,r:W*0.09*(1+0.02*Math.sin(t*0.5+2.4)),fill:rgba(theme.signal,0.16)}),
    R("g",{transform:"rotate("+(-16+Math.sin(t*0.18)*6)+" "+(W*0.5)+" "+(H*0.5)+")"},
      R("rect",{x:W*0.5-W*0.1/2,y:-H*0.2,width:W*0.1,height:H*1.5,fill:rgba(theme.amber,0.1)})),
    R("g",null,Array.from({length:16}).map(function(_,i){return R("line",{key:"v"+i,x1:W*i/16,y1:0,x2:W*i/16,y2:H,stroke:rgba(theme.signal,0.07),strokeWidth:W*0.02});}).concat(Array.from({length:27}).map(function(_,i){return R("line",{key:"h"+i,x1:0,y1:H*i/27,x2:W,y2:H*i/27,stroke:rgba(theme.signal,0.05),strokeWidth:W*0.02});})))
    );
  },
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
