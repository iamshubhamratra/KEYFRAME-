// BIRD SKY — skin for the shared FilmKit stage (services/film_stage.js).
//
// GENERATED by scripts/gen-film-skins.js from the handoff source
// (templete-design/keyframe-handoff/source/world-pack-3.js, template "BirdSky").
// Edit the handoff source and re-run the generator rather than editing this file.
//
// Display Yeseva One / body Mukta. Camera set ["hopU","pushL","zoomOut","pushR","drop","spin"].
// Entrances: title "rise", items "pop".
// Owns the Scroll:ticker, Toggle:switch, Notify:side, Morph:fade mechanics.
//
// palette / icon / World below are the AUTHORED functions, emitted verbatim — the world in
// particular is re-evaluated per seek against an SVG-DOM shim, so the backdrop is not a
// reinterpretation of the original, it IS the original.

const stage = require("../film_stage");

const SKIN = {
  id: "bird-sky",
  label: "Bird Sky",

  // ---- type ----
  display: "Yeseva One", displayFallback: "serif",
  body: "Mukta", bodyFallback: "sans-serif",
  mono: null,
  em: 0.56,
  titleLine: 1.08, titleSpace: "0",
  titlePreset: "rise", itemPreset: "pop",

  // ---- palette ----
  // The authored function. Called with {} to get the design's own defaults; the brand's
  // accents are then hue-mapped onto `accents` slot by slot at their authored luminance,
  // and every other stop rotates onto the brand's lead hue — so a brand recolours the whole
  // world, not just the headline.
  palette: (t) => ({ sky: t.sky || "#cfe6f2", ink: "#2e3440", sun: "#f2c14e", coral: t.coral || "#e2705c", paper: "#fbf9f4" }),
  accents: ["coral","sun","sky"],
  groundKey: "sky", inkKey: "ink", paperKey: "paper",
  dark: false,

  // ---- per-beat look (verbatim) ----
  look: {
    "hook": {
      "bg": "sky",
      "fg": "ink",
      "hi": "coral",
      "world": true,
      "top": 310,
      "size": 122,
      "kicker": {
        "v": "pill",
        "bg": "ink",
        "c": "paper"
      }
    },
    "statement": {
      "bg": "ink",
      "fg": "paper",
      "hi": "sun",
      "world": false,
      "top": 640,
      "size": 148
    },
    "feature": {
      "bg": "sky",
      "fg": "ink",
      "hi": "coral",
      "world": true,
      "top": 246,
      "size": 96,
      "card": {
        "v": "tilt",
        "bg": "paper",
        "r": 24,
        "line": "ink"
      },
      "chips": {
        "v": "pill",
        "colors": [
          "coral",
          "ink"
        ],
        "text": "paper"
      }
    },
    "montage": {
      "bg": "ink",
      "fg": "paper",
      "hi": "sun",
      "world": false,
      "top": 258,
      "size": 94,
      "tile": {
        "bg": "paper",
        "line": "ink",
        "label": "paper",
        "r": 18
      },
      "tilts": [
        -2.5,
        2,
        1.5,
        -2
      ]
    },
    "stats": {
      "bg": "coral",
      "fg": "paper",
      "hi": "sun",
      "world": false,
      "cols": [
        "sun",
        "paper",
        "sky"
      ],
      "num": 154
    },
    "cta": {
      "bg": "sky",
      "fg": "ink",
      "hi": "coral",
      "world": true,
      "top": 480,
      "size": 120,
      "btn": {
        "v": "pill",
        "bg": "coral",
        "c": "paper"
      },
      "logoShape": "circle"
    }
  },

  // ---- camera + motion ----
  cams: ["hopU","pushL","zoomOut","pushR","drop","spin"], camMul: 5, camOff: 1,
  mag: {"rot":0.8,"driftY":12},
  ambient: 1.6,
  energy: 1,
  badge: "circle",
  

  // ---- the mechanics this pack owns ----
  // The handoff's promise is that no two templates share a mechanism. A pack that does not
  // declare a mechanic here never renders it.
  variants: {"Scroll":"ticker","Toggle":"switch","Notify":"side","Morph":"fade"},

  strings: {
    brandName: "Bird Sky",
  },

  // The chrome mark, rendered once at build time.
  icon: (theme, fg, R2) => R2("svg", { width: 26, height: 26, viewBox: "0 0 24 24", fill: "none", stroke: theme.currentBg, strokeWidth: 2.5, strokeLinecap: "round" }, R2("path", { d: "M3 12q4-5 9-1M12 11q4-5 9-1" })),

  // ---- the world ----
  // The authored animated backdrop, verbatim. Pure in (theme, t, progress, utils); no
  // Math.random and no Date, which is what lets it be re-evaluated deterministically on
  // every seek.
  World: (theme, t, p, u) => R("g", null,
      R("circle", { cx: u.W * 0.8, cy: 300, r: 100, fill: theme.sun, opacity: 0.9 }),
      [0, 1, 2].map((i) => R("g", { key: "cl" + i, transform: `translate(${((t * (14 + i * 6) + i * 420) % (u.W + 560)) - 280},${420 + i * 300})`, fill: rgba("#ffffff", 0.8) },
        R("ellipse", { rx: 100 + i * 20, ry: 34 }), R("ellipse", { cx: 64, cy: -16, rx: 54, ry: 26 }), R("ellipse", { cx: -66, cy: -12, rx: 48, ry: 24 }))),
      Array.from({ length: 9 }).map((_, i) => {
        const row = Math.floor(i / 2), side = i % 2 === 0 ? -1 : 1;
        const fx = u.W * 0.5 + ((t * 70) % (u.W * 0.6)) - u.W * 0.3 + side * row * 74;
        const fy = 800 + row * 56 + Math.sin(t * 2.4 + i) * 10;
        const flap = Math.sin(t * 7 + i) * 8;
        return R("path", { key: "b" + i, d: `M ${fx - 22} ${fy - flap} Q ${fx} ${fy + 10} ${fx + 22} ${fy - flap}`, fill: "none", stroke: theme.ink, strokeWidth: 6, strokeLinecap: "round" });
      }),
      R("g", { transform: `translate(${u.W * 0.2 + Math.sin(t * 0.8) * 40},${u.H - 560 + Math.cos(t * 0.6) * 30}) rotate(${Math.sin(t) * 10})` },
        R("path", { d: "M0 -50 L 34 0 L 0 50 L -34 0 Z", fill: theme.coral }),
        R("line", { x1: 0, y1: -50, x2: 0, y2: 50, stroke: rgba("#ffffff", 0.6), strokeWidth: 3 }),
        R("path", { d: `M0 50 q 20 60 -10 120 q ${Math.sin(t * 2) * 20} 40 8 90`, stroke: rgba(theme.ink, 0.5), strokeWidth: 3, fill: "none" })),
      R("path", { d: `M -20 ${u.H - 150} Q ${u.W * 0.3} ${u.H - 230} ${u.W * 0.6} ${u.H - 160} T ${u.W + 20} ${u.H - 190} L ${u.W + 20} ${u.H + 20} L -20 ${u.H + 20} Z`, fill: rgba(theme.ink, 0.85) })),
};

module.exports = {
  buildComposition: (opts) => stage.build(SKIN, opts),
  STRINGS: { ...stage.BASE_STRINGS, ...SKIN.strings },
  SKIN,
};
